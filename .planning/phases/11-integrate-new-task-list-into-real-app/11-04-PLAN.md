---
phase: 11-integrate-new-task-list-into-real-app
plan: 04
type: execute
wave: 4
depends_on: ["11-02", "11-03"]
files_modified:
  - frontend/src/views/TasksView.vue
autonomous: false
requirements:
  - P11-TASKLIST-01

must_haves:
  truths:
    - "TasksView has a permanent 2-panel split: task list left, detail pane right"
    - "Task list supports three rendering modes: flat with drag-drop, grouped by status/priority/project, and tree view"
    - "All existing filters work: status tabs, priority, project, due date, ADO link status"
    - "FilterCycleButton cycles All/ADO/Personal filter"
    - "QuickAddInput appears for new task creation"
    - "TreeTaskRow is used for all task rendering (not old TaskRow)"
    - "Detail pane shows TaskDetail when a task is selected, or empty state when none selected"
  artifacts:
    - path: "frontend/src/views/TasksView.vue"
      provides: "Complete task list view with 2-panel split layout"
      contains: "TreeTaskRow"
      min_lines: 200
  key_links:
    - from: "frontend/src/views/TasksView.vue"
      to: "frontend/src/components/tasks/TreeTaskRow.vue"
      via: "Import and use for all task rendering"
      pattern: "import TreeTaskRow"
    - from: "frontend/src/views/TasksView.vue"
      to: "frontend/src/components/tasks/TaskDetail.vue"
      via: "Import for right panel"
      pattern: "import TaskDetail"
    - from: "frontend/src/views/TasksView.vue"
      to: "frontend/src/stores/tasks.ts"
      via: "enhancedFilteredTasks, groupedEnhanced, filterAdoLink, selectedTaskId"
      pattern: "taskStore\\.enhancedFilteredTasks"
    - from: "frontend/src/views/TasksView.vue"
      to: "frontend/src/composables/useAdoMeta.ts"
      via: "Batch ADO metadata for task rows"
      pattern: "useAdoMeta"
---

<objective>
Rewrite TasksView.vue with the permanent 2-panel split layout, replacing the current implementation while preserving all existing features.

Purpose: This is the final integration task — the main deliverable of Phase 11. The current TasksView uses old TaskRow components and has a mixed slide-out/split panel. The new version uses TreeTaskRow everywhere, has a permanent 2-panel split (list left, detail right), and adds the new FilterCycleButton and QuickAddInput from Plan 02.

Output: Completely rewritten TasksView.vue that is the production task list view.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/11-integrate-new-task-list-into-real-app/11-CONTEXT.md
@.planning/phases/11-integrate-new-task-list-into-real-app/11-RESEARCH.md
@.planning/phases/11-integrate-new-task-list-into-real-app/11-01-SUMMARY.md
@.planning/phases/11-integrate-new-task-list-into-real-app/11-02-SUMMARY.md
@.planning/phases/11-integrate-new-task-list-into-real-app/11-03-SUMMARY.md

@frontend/src/views/TasksView.vue
@frontend/src/components/tasks/TreeTaskRow.vue
@frontend/src/components/tasks/TaskDetail.vue
@frontend/src/components/tasks/QuickAddInput.vue
@frontend/src/components/tasks/FilterCycleButton.vue
@frontend/src/components/FilterBar.vue
@frontend/src/stores/tasks.ts
@frontend/src/types/index.ts

<interfaces>
<!-- TaskStore API -->
```typescript
// From stores/tasks.ts — all available for TasksView
const tasks = ref<Task[]>([])
const loading = ref(false)
const selectedTaskId = ref<number | null>(null)
const filterStatus = ref<string>('all')
const filterPriority = ref<string>('all')
const filterProject = ref<number | null>(null)
const filterDueDate = ref<string>('all')
const filterAdoLink = ref<string>('all')  // wired to FilterCycleButton
const sortBy = ref<string>('priority')
const groupBy = ref<string | null>('project')
const enhancedFilteredTasks = computed<Task[]>()  // excludes subtasks, applies all filters, sorts
const groupedEnhanced = computed<Record<string, Task[]>>()  // groups by current groupBy
function isPublic(taskId: number): boolean
function quickAdd(title: string): Promise<Task>
function selectTask(id: number | null): void
function fetchTasks(): Promise<void>
function reorderTasks(orderedIds: number[]): Promise<void>
function setStatus(id: number, status: string): Promise<Task>
```

<!-- TreeTaskRow Props/Emits -->
```typescript
Props: { task: Task, indentLevel?: number, selected?: boolean, expanded?: boolean,
         hasChildren?: boolean, adoMeta?: {type,state}|null, projectName?: string,
         subtaskProgress?: {done,total,pct}|null, isPublic?: boolean }
Emits: click(), toggleExpand(), toggleStatus(id: number)
```

<!-- FilterBar Props -->
```typescript
Props: { filterStatus, filterPriority, filterProject, filterDueDate, filterAdoLink,
         sortBy, groupBy, treeView, projects, syncing }
Emits: update:filterStatus, update:filterPriority, update:filterProject, update:filterDueDate,
       update:filterAdoLink, update:sortBy, update:groupBy, update:treeView, sync
```

<!-- FilterCycleButton -->
```typescript
Props: { modelValue: string }  // 'all' | 'linked' | 'personal'
Emits: update:modelValue
```

<!-- QuickAddInput -->
```typescript
Emits: add(title: string), cancel()
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite TasksView.vue with permanent 2-panel split layout</name>
  <files>frontend/src/views/TasksView.vue</files>
  <read_first>
    - frontend/src/views/TasksView.vue (current implementation — ALL of it, understand every feature)
    - frontend/src/components/FilterBar.vue (props/emits interface for FilterBar)
    - frontend/src/components/tasks/TreeTaskRow.vue (props/emits for new row component)
    - frontend/src/components/tasks/TaskDetail.vue (rebuilt component from Plan 03)
    - frontend/src/components/tasks/QuickAddInput.vue (Plan 02)
    - frontend/src/components/tasks/FilterCycleButton.vue (Plan 02)
    - frontend/src/stores/tasks.ts (enhancedFilteredTasks, groupedEnhanced, filterAdoLink, all filter refs)
    - frontend/src/composables/useAdoMeta.ts (Plan 01)
    - frontend/src/views/playground/PlaygroundIntegrated.vue (layout reference)
  </read_first>
  <action>
Completely rewrite `frontend/src/views/TasksView.vue`. This is a full replacement — delete all existing content and write from scratch, but PRESERVE all features from the current implementation.

**Features to preserve from current TasksView (EVERY ONE of these must work):**
1. Status chip tabs in topbar via Teleport (All, Todo, In Progress, In Review, Done, Blocked)
2. Sync button in topbar via Teleport (RefreshCw icon, spins when syncing)
3. FilterBar with all dropdowns (priority, project, dueDate, adoLink, sortBy, groupBy, treeView toggle)
4. Three rendering modes: flat with drag-drop, grouped by dimension, tree view
5. Drag-drop reordering in flat mode (vuedraggable with drag handle)
6. Group-by mode with collapsible group headers (project ADO type icons, badges, task counts)
7. Tree view with expand/collapse (parent → child → grandchild)
8. Quick-add task input
9. Loading skeleton state
10. Empty state
11. Keyboard shortcuts (Cmd+N / Ctrl+N, Escape)
12. Project detail panel (click project group header → ProjectDetail)
13. Right panel: TaskDetail when task selected, ProjectDetail when project selected, empty placeholder otherwise

**New features to ADD:**
1. Use TreeTaskRow instead of TaskRow everywhere
2. Use FilterCycleButton in the topbar area (next to status chips)
3. Use QuickAddInput component (not raw Input)
4. Pass adoMeta to every TreeTaskRow via `useAdoMeta().getAdoMeta(task.id)`
5. Pass subtaskProgress to TreeTaskRow in tree/flat modes
6. Pass isPublic to TreeTaskRow via `taskStore.isPublic(task.id)`

**Script setup section:**

```typescript
import { ref, onMounted, onActivated, onDeactivated, computed, watch } from 'vue'
import { useRoute } from 'vue-router'
import { useMagicKeys, whenever } from '@vueuse/core'
import draggable from 'vuedraggable'
import { useTaskStore, type Task } from '@/stores/tasks'
import { useProjectStore } from '@/stores/projects'
import { useADOStore } from '@/stores/ado'
import { useSyncStore } from '@/stores/sync'
import { usePRStore } from '@/stores/prs'
import { useAdoMeta } from '@/composables/useAdoMeta'
import TaskDetail from '@/components/tasks/TaskDetail.vue'
import ProjectDetail from '@/components/projects/ProjectDetail.vue'
import TreeTaskRow from '@/components/tasks/TreeTaskRow.vue'
import FilterBar from '@/components/FilterBar.vue'
import FilterCycleButton from '@/components/tasks/FilterCycleButton.vue'
import QuickAddInput from '@/components/tasks/QuickAddInput.vue'
import AzureDevOpsIcon from '@/components/icons/AzureDevOpsIcon.vue'
import { adoTypeColor, adoTypeIcon } from '@/lib/styles'
import { ClipboardList, RefreshCw, ChevronRight, ChevronDown } from 'lucide-vue-next'
import { Skeleton } from '@/components/ui/skeleton'
import EmptyState from '@/components/EmptyState.vue'
import { Button } from '@/components/ui/button'
import { Badge } from '@/components/ui/badge'
import { ScrollArea } from '@/components/ui/scroll-area'
```

Initialize stores + composable:
```typescript
const route = useRoute()
const taskStore = useTaskStore()
const projectStore = useProjectStore()
const adoStore = useADOStore()
const syncStore = useSyncStore()
const prStore = usePRStore()
const adoMeta = useAdoMeta()
```

Keep these refs/computeds from current implementation (copy verbatim):
- `isActive` ref with onActivated/onDeactivated/onMounted
- `showQuickAdd` ref (toggled by button, hides after add)
- `expandedGroups` ref (Set<string> for group collapse)
- `selectedProjectId` ref (for ProjectDetail)
- `treeView` ref (boolean toggle)
- `expandedTreeNodes` ref (Set<number>)
- `statusChips` array
- `projectNameMap` computed
- `subtaskCounts` computed
- `subtasksByParent` computed
- `subtasksFor(taskId)` function
- `subtaskProgress(taskId)` function
- `toggleSubtasks(taskId)` function
- `groupedTasks` computed (from `taskStore.groupedEnhanced`)
- `groupKeys` computed (with ordering logic)
- `groupLabel(key)` function
- `draggableList` writable computed
- `isDragging` ref
- `selectTask(id)` function
- `closeDetail()` function
- `toggleDone(task)` function
- `handleQuickAdd(title)` — now takes title string from QuickAddInput emit
- `rootTasks` computed
- `treeChildren(taskId)` function
- `hasTreeChildren(taskId)` function
- `toggleTreeNode(id)` function
- `treeSubtaskProgress(taskId)` function
- `projectAdoLookup` computed
- `typeIcon(type)` function
- `hasAnyTasks` computed
- Keyboard shortcuts (Meta_n, Ctrl_n, onEscape)
- `onMounted` auto-fetch + auto-select first task
- `watch(route.query.create, ...)` for ?create=1

**Template structure:**

```html
<div class="flex-1 flex overflow-hidden" @keydown.esc="onEscape" tabindex="-1">
  <!-- Teleport to topbar: status chips + FilterCycleButton + sync -->
  <Teleport v-if="isActive" to="#topbar-actions">
    <div class="flex items-center gap-1">
      <Badge v-for="chip in statusChips" :key="chip.id" ... @click="taskStore.filterStatus = chip.id">
        {{ chip.label }}
      </Badge>
      <FilterCycleButton v-model="taskStore.filterAdoLink" />
      <Button variant="ghost" size="icon" ... @click="syncStore.manualSync()">
        <RefreshCw :size="12" :class="{ 'animate-spin': syncStore.syncing }" />
      </Button>
    </div>
  </Teleport>

  <!-- LEFT: Task list panel -->
  <div class="flex-1 flex flex-col min-w-0 w-[55%]">
    <FilterBar ... />  <!-- same as current -->

    <ScrollArea class="flex-1 h-full">
      <!-- Loading skeleton -->
      <!-- Empty state -->
      <!-- Quick-add (always at top) -->
      <QuickAddInput v-if="showQuickAdd" @add="handleQuickAdd" @cancel="showQuickAdd = false" />

      <!-- OR use always-visible input like current: -->
      <div class="px-4 py-2 border-b border-border/50">
        <QuickAddInput @add="handleQuickAdd" @cancel="() => {}" />
      </div>

      <!-- Grouped rendering (REPLACE TaskRow → TreeTaskRow in every loop) -->
      <!-- Group headers: keep exact same structure with project ADO icons -->
      <!-- Inside each group v-for loop: -->
      <TreeTaskRow
        v-for="task in groupedTasks[key]" :key="task.id"
        :task="task"
        :is-public="taskStore.isPublic(task.id)"
        :selected="taskStore.selectedTaskId === task.id"
        :ado-meta="adoMeta.getAdoMeta(task.id)"
        :project-name="task.projectId ? projectNameMap[task.projectId] : undefined"
        :subtask-progress="subtaskProgress(task.id)"
        @click="selectTask(task.id)"
        @toggle-status="(id) => { const t = taskStore.tasks.find(x => x.id === id); if (t) toggleDone(t) }"
      />

      <!-- Tree rendering (REPLACE inline tree markup → TreeTaskRow with indentLevel) -->
      <template v-for="task in rootTasks" :key="task.id">
        <TreeTaskRow
          :task="task"
          :indent-level="0"
          :selected="taskStore.selectedTaskId === task.id"
          :expanded="expandedTreeNodes.has(task.id)"
          :has-children="hasTreeChildren(task.id)"
          :ado-meta="adoMeta.getAdoMeta(task.id)"
          :project-name="task.projectId ? projectNameMap[task.projectId] : undefined"
          :subtask-progress="treeSubtaskProgress(task.id)"
          :is-public="taskStore.isPublic(task.id)"
          @click="selectTask(task.id)"
          @toggle-expand="toggleTreeNode(task.id)"
          @toggle-status="(id) => { const t = taskStore.tasks.find(x => x.id === id); if (t) toggleDone(t) }"
        />

        <!-- Children level 1 -->
        <template v-if="expandedTreeNodes.has(task.id)">
          <template v-for="child in treeChildren(task.id)" :key="child.id">
            <TreeTaskRow
              :task="child"
              :indent-level="1"
              :selected="taskStore.selectedTaskId === child.id"
              :expanded="expandedTreeNodes.has(child.id)"
              :has-children="hasTreeChildren(child.id)"
              :ado-meta="adoMeta.getAdoMeta(child.id)"
              :is-public="taskStore.isPublic(child.id)"
              @click="selectTask(child.id)"
              @toggle-expand="toggleTreeNode(child.id)"
              @toggle-status="..."
            />

            <!-- Grandchildren level 2 -->
            <template v-if="expandedTreeNodes.has(child.id)">
              <TreeTaskRow v-for="gc in treeChildren(child.id)" :key="gc.id"
                :task="gc" :indent-level="2"
                :selected="taskStore.selectedTaskId === gc.id"
                :ado-meta="adoMeta.getAdoMeta(gc.id)"
                :is-public="taskStore.isPublic(gc.id)"
                @click="selectTask(gc.id)"
                @toggle-status="..."
              />
            </template>
          </template>
        </template>
      </template>

      <!-- Flat rendering with drag-drop (REPLACE TaskRow → TreeTaskRow inside draggable) -->
      <draggable v-model="draggableList" item-key="id" handle=".drag-handle" ...>
        <template #item="{ element: task }">
          <div class="flex items-center">
            <div class="drag-handle ..."><!-- grip dots SVG --></div>
            <div class="flex-1 min-w-0">
              <TreeTaskRow
                :task="task"
                :is-public="taskStore.isPublic(task.id)"
                :selected="taskStore.selectedTaskId === task.id"
                :ado-meta="adoMeta.getAdoMeta(task.id)"
                :project-name="task.projectId ? projectNameMap[task.projectId] : undefined"
                :subtask-progress="subtaskProgress(task.id)"
                @click="selectTask(task.id)"
                @toggle-status="..."
              />
            </div>
          </div>
        </template>
      </draggable>
    </ScrollArea>
  </div>

  <!-- RIGHT: Detail panel (permanent, not slide-out) -->
  <ProjectDetail v-if="selectedProjectId" ... />
  <TaskDetail v-else-if="taskStore.selectedTask" @close="closeDetail" />
  <div v-else class="w-[45%] shrink-0 border-l border-border flex items-center justify-center">
    <p class="text-sm text-muted-foreground">Select a task or project</p>
  </div>
</div>
```

**handleQuickAdd update:**
Change from using `quickAddTitle.value` to accepting the title string from QuickAddInput emit:
```typescript
function handleQuickAdd(title: string) {
  taskStore.quickAdd(title)
}
```

**CRITICAL: Do NOT remove any existing feature.** Every filter, every keyboard shortcut, every rendering mode, every group header element must be preserved. The only changes are:
- TaskRow → TreeTaskRow (with new props)
- Add FilterCycleButton to topbar Teleport
- Use QuickAddInput component (or keep the always-visible Input — either approach is fine, match current UX)
- Add adoMeta prop to every TreeTaskRow
- Add subtaskProgress and isPublic props to TreeTaskRow
  </action>
  <verify>
    <automated>cd C:\code\xb-tasks\frontend && npx vue-tsc --noEmit --skipLibCheck 2>&1 | Select-String -Pattern "error" | Select-Object -First 10</automated>
  </verify>
  <acceptance_criteria>
    - frontend/src/views/TasksView.vue contains `import TreeTaskRow from '@/components/tasks/TreeTaskRow.vue'`
    - TasksView.vue contains `import { useAdoMeta } from '@/composables/useAdoMeta'`
    - TasksView.vue contains `import FilterCycleButton from '@/components/tasks/FilterCycleButton.vue'`
    - TasksView.vue contains `const adoMeta = useAdoMeta()`
    - TasksView.vue contains `<TreeTaskRow` (multiple instances)
    - TasksView.vue does NOT contain `import TaskRow from` (old import removed)
    - TasksView.vue does NOT contain `<TaskRow` (all old usages replaced)
    - TasksView.vue contains `<FilterBar` (FilterBar preserved)
    - TasksView.vue contains `<FilterCycleButton` (new component used)
    - TasksView.vue contains `taskStore.filterAdoLink` (wired to FilterCycleButton)
    - TasksView.vue contains `adoMeta.getAdoMeta(` (ADO metadata passed to rows)
    - TasksView.vue contains `<draggable` (drag-drop preserved)
    - TasksView.vue contains `Teleport` and `#topbar-actions` (topbar teleport preserved)
    - TasksView.vue contains `useMagicKeys` (keyboard shortcuts preserved)
    - TasksView.vue contains `<TaskDetail` (detail panel wired)
    - TasksView.vue contains `<ProjectDetail` (project detail preserved)
    - TasksView.vue contains `groupedTasks` (group-by mode preserved)
    - TasksView.vue contains `expandedTreeNodes` (tree mode preserved)
    - TasksView.vue contains `enhancedFilteredTasks` (enhanced filtering used)
  </acceptance_criteria>
  <done>TasksView uses TreeTaskRow for all task rendering, has permanent 2-panel split layout, preserves all existing features (filters, group-by, tree view, drag-drop, keyboard shortcuts, teleport), adds FilterCycleButton and adoMeta integration</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Complete Phase 11 integration: TasksView rewritten with 2-panel split, TreeTaskRow used everywhere, TaskDetail rebuilt with 3 tabs, DashboardView updated, backend ADO metadata cache wired</what-built>
  <how-to-verify>
    1. Navigate to /tasks — verify permanent 2-panel split (list left, detail right)
    2. Click a task — verify detail pane shows on right with 3 tabs (Subtasks, PRs, Notes)
    3. Click between tasks — verify detail updates (no slide-out animation)
    4. Toggle tree/flat view — verify both modes render correctly with TreeTaskRow
    5. Toggle group-by (status/priority/project) — verify groups with headers work
    6. Test drag-drop in flat mode — verify reordering works
    7. Test All/ADO/Personal filter cycle button — verify task list filters
    8. Test all filter dropdowns (priority, project, due date, ADO link)
    9. Test quick-add — enter a title, press Enter, verify task appears
    10. Navigate to /dashboard — verify TreeTaskRow used (new styling with ADO type icons)
    11. Press Ctrl+N or Cmd+N — verify keyboard shortcut works
    12. Verify no TypeScript or console errors
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
- TasksView.vue uses TreeTaskRow for ALL task rows (flat, grouped, tree modes)
- TasksView.vue has no references to old TaskRow component
- All 12 current features preserved (filters, group-by, tree view, drag-drop, shortcuts, etc.)
- FilterCycleButton wired to taskStore.filterAdoLink
- adoMeta passed to every TreeTaskRow instance
- Detail panel is permanent (not slide-out)
- Human verification confirms visual correctness
</verification>

<success_criteria>
- User can see task list on left, detail on right (permanent split)
- User can switch between flat, grouped, and tree views
- User can filter by All/ADO/Personal via cycle button
- All existing filters (status, priority, project, due date, ADO link) still work
- Drag-drop reordering works in flat mode
- Quick-add creates tasks
- Detail pane has Subtasks/PRs/Notes tabs
- Keyboard shortcuts work (Ctrl+N, Escape)
- No regression from current TasksView functionality
</success_criteria>

<output>
After completion, create `.planning/phases/11-integrate-new-task-list-into-real-app/11-04-SUMMARY.md`
</output>
