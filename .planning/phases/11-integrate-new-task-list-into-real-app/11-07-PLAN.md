---
phase: 11-integrate-new-task-list-into-real-app
plan: 07
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/views/DashboardView.vue
autonomous: false
requirements: [P11-DASHBOARD-01]
gap_closure: true

must_haves:
  truths:
    - "Dashboard Focus/Upcoming/Blocked sections use TreeTaskRow instead of DashboardTaskRow"
    - "TreeTaskRow in Dashboard receives adoMeta, isPublic, subtaskProgress props"
    - "Status toggle works when clicking the status icon on a task row"
    - "Blocked section uses TreeTaskRow component (not inline markup)"
  artifacts:
    - path: "frontend/src/views/DashboardView.vue"
      provides: "Dashboard with TreeTaskRow in all task sections"
  key_links:
    - from: "DashboardView TreeTaskRow"
      to: "useAdoMeta composable"
      via: "adoMeta.getAdoMeta(task.id)"
      pattern: "adoMeta\\.getAdoMeta"
    - from: "DashboardView TreeTaskRow"
      to: "taskStore.isPublic"
      via: ":is-public prop"
      pattern: "isPublic.*task\\.id"
---

<objective>
Replace DashboardTaskRow with TreeTaskRow in all Dashboard task sections and verify status toggle works.

Purpose: UAT test 9 ("Dashboard view renders task rows with new TreeTaskRow styling") failed because DashboardView still uses the old DashboardTaskRow component. The 11-03-SUMMARY claimed this was done but the actual code shows DashboardTaskRow is still imported and rendered. Also verify UAT test 10 (status toggle) which likely works after bea5a5a fixes.
Output: DashboardView.vue with TreeTaskRow in Focus/Upcoming/Blocked sections.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/11-integrate-new-task-list-into-real-app/11-UAT.md
@.planning/phases/11-integrate-new-task-list-into-real-app/11-03-SUMMARY.md

@frontend/src/views/DashboardView.vue
@frontend/src/components/tasks/TreeTaskRow.vue
@frontend/src/components/tasks/DashboardTaskRow.vue

<interfaces>
<!-- TreeTaskRow props interface -->

From frontend/src/components/tasks/TreeTaskRow.vue:
```typescript
interface Props {
  task: Task
  indentLevel?: number       // default 0
  selected?: boolean         // default false
  expanded?: boolean         // default false
  hasChildren?: boolean      // default false
  adoMeta?: { type: string; state: string } | null  // default null
  projectName?: string       // default undefined
  subtaskProgress?: { done: number; total: number; pct: number } | null  // default null
  isPublic?: boolean         // default false
}

// Emits:
//   click: []
//   toggleExpand: []
//   toggleStatus: [id: number]
```

From frontend/src/components/tasks/DashboardTaskRow.vue (to be replaced):
```typescript
interface Props {
  task: Task
  isPersonal?: boolean      // default true
  showDueDate?: boolean     // default false
}

// Emits:
//   click: [taskId: number]    ← NOTE: different signature from TreeTaskRow
```

Key difference: DashboardTaskRow emits `click` with `taskId: number`.
TreeTaskRow emits `click` with no args — caller must provide task.id in the handler.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace DashboardTaskRow with TreeTaskRow in all Dashboard sections</name>
  <files>frontend/src/views/DashboardView.vue</files>
  <action>
**Gap G — Dashboard uses old DashboardTaskRow.**

The DashboardView currently uses `DashboardTaskRow` in Today's Focus (line 326) and Upcoming (line 349) sections, and uses inline markup for the Blocked section (lines 372-388). All three need to use TreeTaskRow.

**Step 1: Swap imports.**

Replace:
```typescript
import DashboardTaskRow from '@/components/tasks/DashboardTaskRow.vue'
```
With:
```typescript
import TreeTaskRow from '@/components/tasks/TreeTaskRow.vue'
```

Also add imports needed for subtask progress computation:
```typescript
import { useProjectStore } from '@/stores/projects'
```

Add to setup:
```typescript
const projectStore = useProjectStore()
```

Add helper computeds:
```typescript
// Subtask counts for TreeTaskRow
const subtaskCounts = computed(() => {
  const counts: Record<number, { done: number; total: number; pct: number }> = {}
  for (const t of taskStore.tasks) {
    if (t.parentId) {
      if (!counts[t.parentId]) counts[t.parentId] = { done: 0, total: 0, pct: 0 }
      counts[t.parentId].total++
      if (t.status === 'done') counts[t.parentId].done++
    }
  }
  // Compute pct
  for (const k of Object.keys(counts)) {
    const c = counts[Number(k)]
    c.pct = c.total > 0 ? Math.round((c.done / c.total) * 100) : 0
  }
  return counts
})

function subtaskProgress(taskId: number) {
  return subtaskCounts.value[taskId] ?? null
}

// Project name lookup
const projectNameMap = computed(() => {
  const map: Record<number, string> = {}
  for (const p of projectStore.projects) {
    map[p.id] = p.name
  }
  return map
})
```

**Step 2: Replace DashboardTaskRow in Today's Focus section (around lines 326-333).**

Replace:
```html
<DashboardTaskRow
  v-for="(task, idx) in focusTasks"
  :key="task.id"
  :task="task"
  :is-personal="!taskStore.isPublic(task.id)"
  :class="idx < focusTasks.length - 1 ? 'border-b border-border' : ''"
  @click="selectTask"
/>
```
With:
```html
<TreeTaskRow
  v-for="(task, idx) in focusTasks"
  :key="task.id"
  :task="task"
  :is-public="taskStore.isPublic(task.id)"
  :ado-meta="adoMeta.getAdoMeta(task.id)"
  :project-name="task.projectId ? projectNameMap[task.projectId] : undefined"
  :subtask-progress="subtaskProgress(task.id)"
  :class="idx < focusTasks.length - 1 ? 'border-b border-border' : ''"
  @click="selectTask(task.id)"
  @toggle-status="(id: number) => taskStore.setStatus(id, taskStore.tasks.find(t => t.id === id)?.status === 'done' ? 'todo' : 'done')"
/>
```

Note the key emit difference: DashboardTaskRow emits `click` with taskId, but TreeTaskRow emits `click` with no args. So we change `@click="selectTask"` to `@click="selectTask(task.id)"`.

**Step 3: Replace DashboardTaskRow in Upcoming section (around lines 349-356).**

Replace:
```html
<DashboardTaskRow
  v-for="(task, idx) in upcomingTasks"
  :key="task.id"
  :task="task"
  :is-personal="!taskStore.isPublic(task.id)"
  show-due-date
  :class="idx < upcomingTasks.length - 1 ? 'border-b border-border' : ''"
  @click="selectTask"
/>
```
With:
```html
<TreeTaskRow
  v-for="(task, idx) in upcomingTasks"
  :key="task.id"
  :task="task"
  :is-public="taskStore.isPublic(task.id)"
  :ado-meta="adoMeta.getAdoMeta(task.id)"
  :project-name="task.projectId ? projectNameMap[task.projectId] : undefined"
  :subtask-progress="subtaskProgress(task.id)"
  :class="idx < upcomingTasks.length - 1 ? 'border-b border-border' : ''"
  @click="selectTask(task.id)"
  @toggle-status="(id: number) => taskStore.setStatus(id, taskStore.tasks.find(t => t.id === id)?.status === 'done' ? 'todo' : 'done')"
/>
```

Note: `show-due-date` prop is dropped — TreeTaskRow always shows due date when present (built into its Row 2 metadata).

**Step 4: Replace inline blocked-task markup with TreeTaskRow (around lines 370-389).**

Replace the entire inline blocked section content:
```html
<div
  v-for="(task, idx) in blockedTasks"
  :key="task.id"
  class="flex flex-col gap-1 px-4 py-2.5 cursor-pointer hover:bg-muted/50 transition-colors"
  :class="idx < blockedTasks.length - 1 ? 'border-b border-border' : ''"
  @click="selectTask(task.id)"
>
  <div class="flex items-center gap-2">
    <span :class="['size-2 rounded-full shrink-0', priorityBgColor(task.priority)]" />
    <span class="text-sm flex-1 truncate text-foreground">{{ task.title }}</span>
  </div>
  <p v-if="task.blockedReason"
    class="text-[11px] text-red-500/70 pl-4 italic">
    {{ task.blockedReason }}
  </p>
</div>
```

With:
```html
<TreeTaskRow
  v-for="(task, idx) in blockedTasks"
  :key="task.id"
  :task="task"
  :is-public="taskStore.isPublic(task.id)"
  :ado-meta="adoMeta.getAdoMeta(task.id)"
  :project-name="task.projectId ? projectNameMap[task.projectId] : undefined"
  :subtask-progress="subtaskProgress(task.id)"
  :class="idx < blockedTasks.length - 1 ? 'border-b border-border' : ''"
  @click="selectTask(task.id)"
  @toggle-status="(id: number) => taskStore.setStatus(id, taskStore.tasks.find(t => t.id === id)?.status === 'done' ? 'todo' : 'done')"
/>
```

TreeTaskRow already has Row 3 blocked banner rendering (`v-if="task.status === 'blocked' && task.blockedReason"`), so the blocked reason display is handled automatically.

**Step 5: Remove unused imports.**

Remove `import DashboardTaskRow from ...` (now replaced).
The `priorityBgColor` import can be removed if it was ONLY used for the inline blocked section. Check if it's used elsewhere — if `statCards` or any other section uses it, keep it. If only the blocked section used it, remove it.

**Step 6: Also add `projectStore.fetchProjects()` to onMounted if not present**, to ensure project names are available:
```typescript
if (!projectStore.projects.length) projectStore.fetchProjects()
```
  </action>
  <verify>
    <automated>cd frontend && npx vue-tsc --noEmit --skipLibCheck 2>&1 | Select-String -Pattern "error TS" | Select-Object -First 10</automated>
  </verify>
  <done>
    - DashboardView has zero references to DashboardTaskRow
    - All three sections (Focus, Upcoming, Blocked) use TreeTaskRow
    - TreeTaskRow receives adoMeta, isPublic, subtaskProgress, projectName props
    - @click handlers pass task.id explicitly (not relying on emit payload)
    - @toggle-status handler wired for status toggling
    - TypeScript compiles without new errors
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify all gap closure fixes visually</name>
  <files>frontend/src/views/TasksView.vue, frontend/src/components/tasks/TaskDetail.vue, frontend/src/views/DashboardView.vue</files>
  <action>
    Verify all gap closure fixes from plans 05-07:
    - Plan 05: Drag handles visible, adaptive group headers, task list header, expand/collapse, no duplicate Scope
    - Plan 06: TaskDetail subtask filter cycle, ADO type icons, pipeline status per PR, dirty-field sync indicators
    - Plan 07: Dashboard uses TreeTaskRow everywhere

    Steps to test:
    1. Open /tasks — verify drag handle dots visible in flat mode (6-dot grip pattern on left of each row)
    2. Group by Status — verify headers show colored status dots (not project ADO icons)
    3. Group by Priority — verify headers show colored priority dots
    4. Group by Project — verify all groups show including ungrouped tasks
    5. Verify task list header shows task count + FilterCycleButton
    6. Click Expand All / Collapse All buttons — verify they work
    7. Click a task — verify TaskDetail subtask section has filter cycle button
    8. Verify TaskDetail PR section shows pipeline icons under PRs (if pipelines exist)
    9. Verify TaskDetail ADO bar shows "Synced" or "{N} pending" appropriately
    10. Open /dashboard — verify Focus/Upcoming/Blocked sections use new task row styling (status circles, ADO icons)
    11. Click a status icon circle — verify it toggles the task status
  </action>
  <verify>User approves visual verification</verify>
  <done>All 11 verification steps pass. User types "approved".</done>
</task>

</tasks>

<verification>
1. `cd frontend && npx vue-tsc --noEmit --skipLibCheck` — no new TS errors
2. `Select-String -Path "frontend/src/views/DashboardView.vue" -Pattern "DashboardTaskRow"` returns empty
3. `Select-String -Path "frontend/src/views/DashboardView.vue" -Pattern "TreeTaskRow"` returns matches
4. `Select-String -Path "frontend/src/views/DashboardView.vue" -Pattern "adoMeta.getAdoMeta"` returns matches
5. `Select-String -Path "frontend/src/views/DashboardView.vue" -Pattern "toggle-status"` returns matches
</verification>

<success_criteria>
- Dashboard uses TreeTaskRow in all 3 task sections (Focus, Upcoming, Blocked)
- TreeTaskRow props properly wired (adoMeta, isPublic, subtaskProgress, projectName)
- Status toggle works on task rows
- No references to old DashboardTaskRow in DashboardView
- User approves the visual verification checkpoint
</success_criteria>

<output>
After completion, create `.planning/phases/11-integrate-new-task-list-into-real-app/11-07-SUMMARY.md`
</output>
