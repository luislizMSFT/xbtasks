---
phase: 10-implement-dashboard-redesign-and-unified-header-bars
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/SyncCluster.vue
  - frontend/src/lib/styles.ts
  - frontend/src/layouts/AppShell.vue
  - frontend/src/views/DashboardView.vue
  - frontend/src/views/TasksView.vue
  - frontend/src/views/AdoView.vue
  - frontend/src/views/ProjectsView.vue
  - frontend/src/views/DependencyGraphView.vue
  - frontend/src/views/SettingsView.vue
autonomous: true
requirements:
  - P10-HEADER-01

must_haves:
  truths:
    - "Every page shows the SyncCluster in the top bar left zone with green/red dot, Synced/Offline/Syncing label, relative time, and refresh button"
    - "Each page teleports its center zone content to #topbar-center — Dashboard: stat badges, Tasks: filter chips, ADO: tabs, Projects: count+active, Dependencies: node+edge stats, Settings: empty"
    - "No page uses the old #topbar-actions teleport target"
    - "The SyncCluster refresh button calls syncStore.manualSync() and shows spinner while syncing"
  artifacts:
    - path: "frontend/src/components/SyncCluster.vue"
      provides: "Shared sync status + refresh component for AppShell left zone"
      contains: "aria-label=\"Refresh sync\""
    - path: "frontend/src/layouts/AppShell.vue"
      provides: "3-zone top bar: left (page name + divider + SyncCluster), center (#topbar-center), right (search+new+activity)"
      contains: "topbar-center"
    - path: "frontend/src/lib/styles.ts"
      provides: "priorityBgColor() helper for priority dot backgrounds"
      exports: ["priorityBgColor"]
  key_links:
    - from: "frontend/src/components/SyncCluster.vue"
      to: "stores/sync.ts + stores/ado.ts"
      via: "useSyncStore() + useADOStore()"
      pattern: "syncStore\\.manualSync|adoStore\\.connected"
    - from: "frontend/src/layouts/AppShell.vue"
      to: "frontend/src/components/SyncCluster.vue"
      via: "import + template usage"
      pattern: "SyncCluster"
    - from: "all 6 views"
      to: "AppShell.vue #topbar-center"
      via: "Teleport"
      pattern: "to=\"#topbar-center\""
---

<objective>
Create the SyncCluster shared component, refactor AppShell.vue to a 3-zone top bar layout, and update all 6 page views to teleport their center zone content to the new `#topbar-center` target. This is the foundation that Plan 10-02 (dashboard redesign) builds on.

Purpose: Establishes the unified header bar pattern across all pages — consistent sync status visibility, page-specific center content, and shared right-zone actions.
Output: SyncCluster.vue component, refactored AppShell with 3-zone layout, all views migrated to #topbar-center teleport.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-implement-dashboard-redesign-and-unified-header-bars/10-CONTEXT.md
@.planning/phases/10-implement-dashboard-redesign-and-unified-header-bars/10-UI-SPEC.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From frontend/src/stores/sync.ts:
```typescript
export const useSyncStore = defineStore('sync', () => {
  const syncing = ref(false)
  const lastSyncedAt = ref<string | null>(null)
  const conflicts = ref<Conflict[]>([])
  // ...
  const hasConflicts = computed(() => conflicts.value.length > 0)
  const conflictCount = computed(() => conflicts.value.length)
  function manualSync(): Promise<void>
})
```

From frontend/src/stores/ado.ts:
```typescript
// adoStore.connected: boolean — ADO connection status (green/red dot)
// adoStore.pipelines: ADOPipeline[] — pipeline data
```

From frontend/src/stores/tasks.ts:
```typescript
const stats = computed(() => ({
  total: tasks.value.length,
  inProgress: tasks.value.filter(t => t.status === 'in_progress').length,
  inReview: tasks.value.filter(t => t.status === 'in_review').length,
  blocked: tasks.value.filter(t => t.status === 'blocked').length,
  done: tasks.value.filter(t => t.status === 'done').length,
  todo: tasks.value.filter(t => t.status === 'todo').length,
}))
```

From frontend/src/lib/date.ts:
```typescript
export function relativeTime(date: string): string
```

From frontend/src/lib/styles.ts:
```typescript
export function priorityColor(priority: string): string  // text color, P0-P3
export function adoTypeIcon(type?: string): Component
export function adoTypeColor(type: string): string
```

Existing teleport pattern (established in Dashboard/Tasks/ADO):
```typescript
const isActive = ref(false)
onActivated(() => { isActive.value = true })
onDeactivated(() => { isActive.value = false })
onMounted(() => { isActive.value = true })
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create SyncCluster component + add priorityBgColor helper</name>
  <files>frontend/src/components/SyncCluster.vue, frontend/src/lib/styles.ts</files>
  <read_first>
    - frontend/src/stores/sync.ts (sync store API: syncing, lastSyncedAt, conflicts, manualSync)
    - frontend/src/stores/ado.ts (ado store: connected state)
    - frontend/src/lib/styles.ts (existing helpers — append new function)
    - frontend/src/lib/date.ts (relativeTime function signature)
    - .planning/phases/10-implement-dashboard-redesign-and-unified-header-bars/10-UI-SPEC.md (SyncCluster spec in Component Inventory and Interaction Contract sections)
  </read_first>
  <action>
**1. Create `frontend/src/components/SyncCluster.vue`**

This is a new shared component used in AppShell's left zone. It shows sync connection status, relative time, pending badge, and refresh button.

Script setup imports:
```typescript
import { computed } from 'vue'
import { useADOStore } from '@/stores/ado'
import { useSyncStore } from '@/stores/sync'
import { relativeTime } from '@/lib/date'
import { Button } from '@/components/ui/button'
import { Badge } from '@/components/ui/badge'
import { RefreshCw, Loader2 } from 'lucide-vue-next'
```

Store setup:
```typescript
const adoStore = useADOStore()
const syncStore = useSyncStore()
const pendingCount = computed(() => syncStore.conflicts.length)
```

Template — exact markup (from approved playground + UI-SPEC):
```html
<template>
  <div class="flex items-center gap-2 text-[11px]">
    <!-- Connection status: dot + label -->
    <span class="inline-flex items-center gap-1.5">
      <span class="w-1.5 h-1.5 rounded-full"
        :class="adoStore.connected ? 'bg-green-500' : 'bg-red-500'" />
      <span :class="[
        !adoStore.connected ? 'text-red-500' :
        syncStore.syncing ? 'text-muted-foreground' : 'text-green-600'
      ]">
        {{ !adoStore.connected ? 'Offline' : syncStore.syncing ? 'Syncing…' : 'Synced' }}
      </span>
    </span>
    <!-- Relative time (only when connected and not syncing) -->
    <span v-if="syncStore.lastSyncedAt && adoStore.connected && !syncStore.syncing"
      class="text-muted-foreground/50 tabular-nums">
      {{ relativeTime(syncStore.lastSyncedAt) }}
    </span>
    <!-- Pending changes badge (only when conflicts > 0) -->
    <Badge v-if="pendingCount > 0" variant="outline"
      class="text-[9px] h-4 px-1.5 gap-1 text-amber-600 border-amber-500/30 bg-amber-500/10">
      <span class="w-1 h-1 rounded-full bg-amber-500" />
      {{ pendingCount }} pending
    </Badge>
    <!-- Refresh button (icon-only, must have aria-label) -->
    <Button variant="ghost" size="sm" class="h-6 w-6 p-0"
      aria-label="Refresh sync"
      @click="syncStore.manualSync()" :disabled="syncStore.syncing">
      <component :is="syncStore.syncing ? Loader2 : RefreshCw"
        :size="12" :class="syncStore.syncing && 'animate-spin'"
        class="text-muted-foreground" />
    </Button>
  </div>
</template>
```

Three states handled:
- Connected + not syncing: green dot, "Synced" (green text), relative time shown
- Connected + syncing: green dot, "Syncing…" (muted text), relative time hidden, Loader2 spinner on button
- Offline: red dot, "Offline" (red text), no time shown

**2. Add `priorityBgColor()` to `frontend/src/lib/styles.ts`**

Append this function after the existing `priorityColor()` function (which is at ~line 68-76):

```typescript
/** Background color for priority dots (P0-P3). Used on dashboard task rows. */
export function priorityBgColor(priority: string): string {
  switch (priority) {
    case 'P0': return 'bg-red-500'
    case 'P1': return 'bg-orange-500'
    case 'P2': return 'bg-amber-500'
    case 'P3': return 'bg-zinc-400'
    default: return 'bg-zinc-400'
  }
}
```
  </action>
  <verify>
    <automated>cd frontend && grep -l "aria-label=\"Refresh sync\"" src/components/SyncCluster.vue && grep -l "adoStore.connected" src/components/SyncCluster.vue && grep -l "Syncing" src/components/SyncCluster.vue && grep "export function priorityBgColor" src/lib/styles.ts && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - File `frontend/src/components/SyncCluster.vue` exists
    - SyncCluster.vue contains `aria-label="Refresh sync"` (icon-only button accessibility)
    - SyncCluster.vue contains `adoStore.connected` (connection status check)
    - SyncCluster.vue contains `syncStore.syncing` (syncing state check)
    - SyncCluster.vue contains the text `Syncing…` (ellipsis character, not three dots)
    - SyncCluster.vue contains `syncStore.manualSync()` (refresh action)
    - SyncCluster.vue contains `pendingCount` computed from `syncStore.conflicts.length`
    - SyncCluster.vue contains `relativeTime` import from `@/lib/date`
    - styles.ts contains `export function priorityBgColor(priority: string): string`
    - styles.ts priorityBgColor returns `bg-red-500` for P0, `bg-orange-500` for P1, `bg-amber-500` for P2, `bg-zinc-400` for P3
  </acceptance_criteria>
  <done>SyncCluster.vue component is complete and renders sync status with all 3 states (connected/syncing/offline). priorityBgColor() is exported from styles.ts.</done>
</task>

<task type="auto">
  <name>Task 2: Refactor AppShell to 3-zone top bar + update all 6 view teleports</name>
  <files>frontend/src/layouts/AppShell.vue, frontend/src/views/DashboardView.vue, frontend/src/views/TasksView.vue, frontend/src/views/AdoView.vue, frontend/src/views/ProjectsView.vue, frontend/src/views/DependencyGraphView.vue, frontend/src/views/SettingsView.vue</files>
  <read_first>
    - frontend/src/layouts/AppShell.vue (current 2-zone top bar — must refactor to 3-zone)
    - frontend/src/views/DashboardView.vue (lines 165-186 — existing teleport to #topbar-actions)
    - frontend/src/views/TasksView.vue (lines 297-318 — existing teleport with status chips + sync button)
    - frontend/src/views/AdoView.vue (lines 246-296 — existing teleport with tabs + sync UI)
    - frontend/src/views/ProjectsView.vue (no teleport yet — needs one)
    - frontend/src/views/DependencyGraphView.vue (no teleport yet — needs one)
    - frontend/src/views/SettingsView.vue (no teleport yet — needs one)
    - frontend/src/components/SyncCluster.vue (just created in Task 1 — confirm it exists)
    - .planning/phases/10-implement-dashboard-redesign-and-unified-header-bars/10-UI-SPEC.md (Layout Contract section for exact 3-zone markup)
  </read_first>
  <action>
**CRITICAL: AppShell rename and ALL view teleport updates MUST happen together. Changing AppShell without updating views causes content to silently disappear (Vue teleport to non-existent target fails silently).**

**A. Refactor `frontend/src/layouts/AppShell.vue` top bar to 3-zone layout**

1. Add import at top of script:
```typescript
import SyncCluster from '@/components/SyncCluster.vue'
```

2. Add `/dependencies` to the breadcrumb map (currently missing):
```typescript
'/dependencies': 'Dependencies',
```

3. Replace the top bar content INSIDE the `<div class="h-[46px] ...">` with the 3-zone layout. The current structure is:
```
[breadcrumb] [#topbar-actions div] [flex-1 spacer] [actions div]
```
Replace with:
```
[breadcrumb] [divider] [SyncCluster] [#topbar-center flex-1] [actions div with shrink-0]
```

Exact template for the top bar inner content (replacing lines 84-127 approximately):
```html
<!-- LEFT ZONE: page name + divider + sync cluster -->
<span class="text-sm font-semibold text-foreground titlebar-no-drag select-none shrink-0">
  {{ breadcrumb }}
</span>
<span class="w-px h-4 bg-border mx-1 shrink-0" />
<SyncCluster class="titlebar-no-drag shrink-0" />

<!-- CENTER ZONE: page-specific teleport target -->
<div id="topbar-center" class="flex-1 flex items-center justify-center gap-2 titlebar-no-drag" />

<!-- RIGHT ZONE: search + new + activity (unchanged, add shrink-0 to wrapper) -->
<div v-if="showActions" class="flex items-center gap-2 titlebar-no-drag shrink-0">
  <!-- Keep ALL existing content inside this div unchanged:
       Search button, New dropdown, Activity toggle button -->
</div>
```

Key changes from current AppShell:
- `font-medium` → `font-semibold` on page name span
- Remove `<div id="topbar-actions" .../>` entirely
- Remove `<div class="flex-1" />` spacer entirely
- Add `<span class="w-px h-4 bg-border mx-1 shrink-0" />` divider after page name
- Add `<SyncCluster class="titlebar-no-drag shrink-0" />` after divider
- Add `<div id="topbar-center" class="flex-1 flex items-center justify-center gap-2 titlebar-no-drag" />` as center zone
- Add `shrink-0` class to the right zone wrapper div
- All other content (search button, New dropdown, Clock/activity button) stays exactly as-is

**B. Update DashboardView.vue teleport (lines ~165)**

Change `to="#topbar-actions"` → `to="#topbar-center"`. Keep existing teleport content for now (will be replaced in Plan 10-02). The teleport line becomes:
```html
<Teleport v-if="isActive" to="#topbar-center">
```

**C. Update TasksView.vue teleport (lines ~297-318)**

1. Change `to="#topbar-actions"` → `to="#topbar-center"`
2. Remove the sync RefreshCw button and its wrapper from the teleport content (lines 308-316). The sync button is now in SyncCluster. Keep only the status filter chips Badge elements.

After update, the teleport should contain ONLY:
```html
<Teleport v-if="isActive" to="#topbar-center">
  <div class="flex items-center gap-1">
    <Badge
      v-for="chip in statusChips"
      :key="chip.id"
      :variant="taskStore.filterStatus === chip.id ? 'default' : 'outline'"
      class="cursor-pointer text-[10px] px-1.5 py-0 h-5 select-none"
      @click="taskStore.filterStatus = chip.id"
    >
      {{ chip.label }}
    </Badge>
  </div>
</Teleport>
```

Also remove `RefreshCw` from the lucide imports if no longer used elsewhere in TasksView (check first — it may be used in other parts of the template).

**D. Update AdoView.vue teleport (lines ~246-296)**

1. Change `to="#topbar-actions"` → `to="#topbar-center"`
2. Remove from the teleport content:
   - The loading spinner: `<RefreshCw v-if="adoStore.loading" :size="12" class="animate-spin text-muted-foreground shrink-0" />`
   - The `lastSyncLabel` span: `<span v-if="lastSyncLabel" ...>`
   - The Connected/Offline pill: `<span :class="cn(..." ...>` (the Wifi/WifiOff status pill)
   - The Sync button: `<Button variant="outline" ...>Sync</Button>`

3. Keep ONLY the `<Tabs>` component with its TabsList and TabsTriggers (Browser, PRs, Pipelines with count badges).

After update, the teleport should contain:
```html
<Teleport v-if="isActive" to="#topbar-center">
  <Tabs v-model="activeTab">
    <TabsList class="h-7 bg-transparent p-0 gap-1">
      <TabsTrigger value="browser" class="text-[11px] gap-1 px-2.5 h-6 data-[state=active]:bg-muted">
        <CheckSquare :size="11" />
        Browser
        <Badge v-if="adoStore.workItemTree.length" variant="secondary" class="text-[9px] h-3.5 px-1">{{ adoStore.workItemTree.length }}</Badge>
      </TabsTrigger>
      <TabsTrigger value="pull-requests" class="text-[11px] gap-1 px-2.5 h-6 data-[state=active]:bg-muted">
        <GitPullRequest :size="11" />
        PRs
        <Badge v-if="totalPRCount" variant="secondary" class="text-[9px] h-3.5 px-1">{{ totalPRCount }}</Badge>
      </TabsTrigger>
      <TabsTrigger value="pipelines" class="text-[11px] gap-1 px-2.5 h-6 data-[state=active]:bg-muted">
        <GitMerge :size="11" />
        Pipelines
        <Badge v-if="adoStore.pipelines.length" variant="secondary" class="text-[9px] h-3.5 px-1">{{ adoStore.pipelines.length }}</Badge>
      </TabsTrigger>
    </TabsList>
  </Tabs>
</Teleport>
```

Also remove unused imports: `Wifi`, `WifiOff`, `Clock` from lucide imports (if no longer used elsewhere in AdoView). Remove `cn` import if no longer used. Keep `handleSync` function — it's still called by the error banner retry section if it exists.

**E. Add teleport to ProjectsView.vue**

1. Add `onActivated, onDeactivated` to the vue import (currently imports `ref, computed, onMounted, watch`):
```typescript
import { ref, computed, onMounted, onActivated, onDeactivated, watch } from 'vue'
```

2. Add isActive ref + lifecycle hooks after existing refs (after `const showLinkDialog = ref(false)` at ~line 31):
```typescript
const isActive = ref(false)
onActivated(() => { isActive.value = true })
onDeactivated(() => { isActive.value = false })
onMounted(() => { isActive.value = true })
```
Note: `onMounted` is already imported and used — keep the existing onMounted call, add a NEW separate onMounted for isActive OR merge into the existing one. Simplest: add `isActive.value = true` to the beginning of the existing onMounted callback.

3. Add `Badge` import if not already present (check — it IS already imported).

4. Add `useTaskStore` import and store instance if not present (check — it IS already imported as `const taskStore = useTaskStore()`).

5. Compute active project count:
```typescript
const activeProjectCount = computed(() => {
  const activeProjectIds = new Set(
    taskStore.tasks
      .filter(t => t.projectId && ['in_progress', 'in_review'].includes(t.status))
      .map(t => t.projectId)
  )
  return activeProjectIds.size
})
```

6. Add Teleport at the START of the template (before `<div class="p-6 space-y-4">`):
```html
<Teleport v-if="isActive" to="#topbar-center">
  <div class="flex items-center gap-2 text-[11px]">
    <span class="text-muted-foreground tabular-nums">{{ projectStore.projects.length }} projects</span>
    <Badge v-if="activeProjectCount > 0" variant="secondary" class="text-[9px] h-4 px-1.5">
      {{ activeProjectCount }} active
    </Badge>
  </div>
</Teleport>
```

**F. Add teleport to DependencyGraphView.vue**

1. Add `onActivated, onDeactivated` to the vue import:
```typescript
import { ref, computed, onMounted, onActivated, onDeactivated } from 'vue'
```

2. Add isActive ref + lifecycle hooks (after existing refs, before `onMounted(loadGraph)`):
```typescript
const isActive = ref(false)
onActivated(() => { isActive.value = true })
onDeactivated(() => { isActive.value = false })
```
And add `isActive.value = true` to the existing `onMounted(loadGraph)` — change to:
```typescript
onMounted(() => {
  isActive.value = true
  loadGraph()
})
```

3. Add edge count and cycle detection computeds (after existing `stats` computed at ~line 347):
```typescript
const edgeCount = computed(() => forceGraphEdges.value.length)

const hasCycle = computed(() => {
  const visited = new Set<number>()
  const stack = new Set<number>()
  function dfs(id: number): boolean {
    if (stack.has(id)) return true
    if (visited.has(id)) return false
    visited.add(id)
    stack.add(id)
    const node = allNodes.value.find(n => n.id === id)
    if (node) {
      for (const depId of node.dependsOn) {
        if (dfs(depId)) return true
      }
    }
    stack.delete(id)
    return false
  }
  return allNodes.value.some(n => dfs(n.id))
})
```

4. Add Teleport at the START of the template (before `<div class="flex h-full flex-col overflow-hidden">`):
```html
<Teleport v-if="isActive" to="#topbar-center">
  <div class="flex items-center gap-2 text-[11px]">
    <span class="text-muted-foreground tabular-nums">{{ stats.total }} nodes · {{ edgeCount }} edges</span>
    <Badge v-if="hasCycle" variant="destructive" class="text-[9px] h-4 px-1.5">
      cycle detected
    </Badge>
  </div>
</Teleport>
```

**G. Add teleport to SettingsView.vue**

1. Add `onActivated, onDeactivated` to the vue import:
```typescript
import { ref, onMounted, onActivated, onDeactivated } from 'vue'
```

2. Add isActive ref + lifecycle hooks (after existing refs):
```typescript
const isActive = ref(false)
onActivated(() => { isActive.value = true })
onDeactivated(() => { isActive.value = false })
```
And add `isActive.value = true` to the existing onMounted callback.

3. Add Teleport at the START of the template (before `<ScrollArea class="flex-1 h-full">`). Settings has no center content, so use an empty teleport:
```html
<Teleport v-if="isActive" to="#topbar-center">
  <span />
</Teleport>
```
  </action>
  <verify>
    <automated>cd frontend && grep "topbar-center" src/layouts/AppShell.vue && grep "SyncCluster" src/layouts/AppShell.vue && grep -L "topbar-actions" src/layouts/AppShell.vue src/views/DashboardView.vue src/views/TasksView.vue src/views/AdoView.vue | wc -l | findstr "4" && grep "topbar-center" src/views/ProjectsView.vue && grep "topbar-center" src/views/DependencyGraphView.vue && grep "topbar-center" src/views/SettingsView.vue && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - AppShell.vue contains `id="topbar-center"` (center zone teleport target)
    - AppShell.vue does NOT contain `id="topbar-actions"` (old target removed)
    - AppShell.vue contains `SyncCluster` import and template usage
    - AppShell.vue breadcrumb map includes `'/dependencies': 'Dependencies'`
    - AppShell.vue page name span uses `font-semibold` (not `font-medium`)
    - AppShell.vue has a divider span `w-px h-4 bg-border` between page name and SyncCluster
    - AppShell.vue right zone div has `shrink-0` class
    - DashboardView.vue contains `to="#topbar-center"` (not `#topbar-actions`)
    - TasksView.vue contains `to="#topbar-center"` (not `#topbar-actions`)
    - TasksView.vue teleport does NOT contain a RefreshCw/sync button (moved to SyncCluster)
    - AdoView.vue contains `to="#topbar-center"` (not `#topbar-actions`)
    - AdoView.vue teleport contains ONLY the Tabs component (no Connected/Offline pill, no Sync button, no lastSyncLabel)
    - ProjectsView.vue contains `to="#topbar-center"` with project count and active badge
    - ProjectsView.vue has `isActive` ref with onActivated/onDeactivated pattern
    - DependencyGraphView.vue contains `to="#topbar-center"` with node/edge stats
    - DependencyGraphView.vue has `edgeCount` and `hasCycle` computed properties
    - SettingsView.vue contains `to="#topbar-center"` (empty center content)
    - SettingsView.vue has `isActive` ref with lifecycle hooks
  </acceptance_criteria>
  <done>AppShell shows 3-zone header. All 6 pages teleport their center zone content to #topbar-center. SyncCluster is visible on every page in the left zone. No references to old #topbar-actions remain.</done>
</task>

</tasks>

<verification>
1. Run `cd frontend && npx vue-tsc --noEmit` — no TypeScript errors from renamed teleport targets or new imports
2. Grep check: `grep -r "topbar-actions" frontend/src/` should return ZERO results (old target fully removed)
3. Grep check: `grep -r "topbar-center" frontend/src/` should return results in AppShell + all 6 views
4. Grep check: `grep -r "SyncCluster" frontend/src/` should return results in AppShell.vue and SyncCluster.vue
</verification>

<success_criteria>
- SyncCluster.vue exists and handles 3 states (connected, syncing, offline) with correct colors and labels per UI-SPEC
- AppShell has 3-zone top bar: left (page name + divider + SyncCluster), center (#topbar-center), right (search+new+activity)
- All 6 views (Dashboard, Tasks, ADO, Projects, Dependencies, Settings) teleport to #topbar-center
- No references to #topbar-actions anywhere in frontend/src/
- priorityBgColor() exported from styles.ts with correct P0-P3 color mapping
- TypeScript compiles without errors
</success_criteria>

<output>
After completion, create `.planning/phases/10-implement-dashboard-redesign-and-unified-header-bars/10-01-SUMMARY.md`
</output>
