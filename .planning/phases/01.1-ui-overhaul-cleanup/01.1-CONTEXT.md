# Phase 1.1: UI Overhaul & Cleanup — Context

## Origin
User-directed insertion. All requirements from conversation on 2026-04-03.

## User Assumptions (verbatim from conversation)

### 1. Playground Cleanup
- PlaygroundDashboardView and PlaygroundTasksView are **superseded** — delete them
- PlaygroundAdoView is used by real `/ado` route — rename to AdoView, keep as production
- PlaygroundChainView → rename to DependencyGraphView (will become the tree visualization)
- PlaygroundShellView → keep as-is or rename to ShellView
- PlaygroundDetailView → delete (TaskDetail component is the real implementation)
- Clean all `/playground/*` routes from router; keep real routes
- Remove playground nav links from Sidebar

### 2. TaskDetail Sidebar Redesign
- **Header**: Status and Priority badges already shown inline at top — make them **dropdowns** (Select) so user can change them right there
- **Remove duplicates**: Status, Priority, and Tags are duplicate in Details footer — remove them from footer
- **Details footer keeps**: Project, Due Date, ADO Link (metadata) + timestamps + Delete button
- **Discussion panel**: The double tab structure (Work/Discussion → Notes/ADO Comments) is confusing. Merge into a better UX:
  - Remove the Work/Discussion top-level tabs
  - Work content (Subtasks, PRs, Description) becomes the main scrollable area
  - Discussion becomes a slide-up bottom panel that overtakes the full sidebar when opened
  - A "Discussion" button/trigger at bottom or in the header opens it
  - Inside Discussion: unified comment stream (Notes + ADO Comments interleaved or sectioned, no sub-tabs)
- **Activity bar**: Already moved to horizontal bar above Details — keep as-is

### 3. Projects Page Redo  
- Current ProjectsView is useless — needs complete redesign
- Should be useful for organizing tasks by project
- Agent should design and implement a functional project management page

### 4. Dependency Graph (Tree Visualization)
- New page/view for visualizing task dependency trees
- Use for tracing where tasks are blocked
- Will also be used for PR dependencies and pipeline tracing in the future
- Replaces/evolves PlaygroundChainView
- Should be interactive — click to expand/trace chains

### 5. API Integration (after UI work)
- Tasks: SQLite local database (already have Go backend services)
- ADO: Piggyback on `az cli` first (user's existing Azure CLI auth)
- Start with read-only ADO data fetch via az cli subprocess calls from Go

## Parallelization Strategy
Plans 1-4 are independent and can execute simultaneously:
- Plan 01: Playground cleanup (file ops, router, sidebar)
- Plan 02: TaskDetail redesign (single component rewrite)
- Plan 03: Projects page redo (new view)
- Plan 04: Dependency graph visualization (new view)

Plan 05 (API integration) depends on Plans 1-4 completing.
