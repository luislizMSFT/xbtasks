---
phase: 02-ado-integration-prs-unified-dashboard
plan: 09
type: execute
wave: 3
depends_on: [02-03, 02-04]
files_modified:
  - frontend/src/components/ExternalLinks.vue
  - frontend/src/components/CommentsSection.vue
  - frontend/src/components/TaskDetail.vue
autonomous: true
requirements: [LINK-03, CMT-01, CMT-02, CMT-03, UX-02]

must_haves:
  truths:
    - "ExternalLinks section shows URLs with auto-detected type icons"
    - "External links open in real browser (not in-app webview)"
    - "CommentsSection shows local comments with public/private indicator"
    - "User can push individual comments to ADO with confirmation"
    - "Public comments visually distinguished from private ones"
    - "Both sections integrated into TaskDetail sidebar"
  artifacts:
    - path: "frontend/src/components/ExternalLinks.vue"
      provides: "Links section with type icons"
      contains: "AddLink"
    - path: "frontend/src/components/CommentsSection.vue"
      provides: "Comments with public/private distinction"
      contains: "PushCommentToADO"
    - path: "frontend/src/components/TaskDetail.vue"
      provides: "Extended detail with links + comments tabs"
      contains: "ExternalLinks"
  key_links:
    - from: "frontend/src/components/ExternalLinks.vue"
      to: "Wails bindings"
      via: "ExternalLinksService methods"
      pattern: "externallinksservice"
    - from: "frontend/src/components/CommentsSection.vue"
      to: "Wails bindings"
      via: "CommentService methods"
      pattern: "commentservice"
---

<objective>
Build the ExternalLinks and CommentsSection components, then integrate them into the TaskDetail sidebar panel.

Purpose: External links make each task an "investigation hub" — ICM incidents, Grafana dashboards, wiki pages all collected in one place. Comments with public/private distinction let users keep notes private while selectively sharing with the ADO team.

Output: `ExternalLinks.vue`, `CommentsSection.vue`, updated `TaskDetail.vue`
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-ado-integration-prs-unified-dashboard/02-CONTEXT.md
@.planning/phases/02-ado-integration-prs-unified-dashboard/02-04-SUMMARY.md

<interfaces>
From internal/app/externallinks.go (Plan 04):
```go
func (s *ExternalLinksService) AddLink(taskID int, url, label string) (domain.TaskLink, error)
func (s *ExternalLinksService) ListLinks(taskID int) ([]domain.TaskLink, error)
func (s *ExternalLinksService) DeleteLink(id int) error
// DetectLinkType is called internally — auto-detects type from URL
```

From internal/app/commentservice.go (Plan 04):
```go
func (s *CommentService) AddComment(taskID int, content string) (domain.TaskComment, error)
func (s *CommentService) ListComments(taskID int) ([]domain.TaskComment, error)
func (s *CommentService) UpdateComment(id int, content string) error
func (s *CommentService) DeleteComment(id int) error
func (s *CommentService) PushCommentToADO(commentID int) error
```

From domain/types.go:
```go
type TaskLink struct {
    ID int; TaskID int; URL string; Label string; Type string; CreatedAt time.Time;
}
type TaskComment struct {
    ID int; TaskID int; Content string; IsPublic bool;
    AdoCommentID string; CreatedAt time.Time; UpdatedAt time.Time;
}
```

From frontend/src/components/TaskDetail.vue (existing):
```
// TaskDetail sidebar — currently has Work/Discussion tabs
// We add External Links section and Comments section
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: ExternalLinks + CommentsSection Components</name>
  <files>frontend/src/components/ExternalLinks.vue, frontend/src/components/CommentsSection.vue</files>
  <read_first>
    - frontend/src/components/TaskDetail.vue (understand current structure, tabs, where links/comments fit)
    - frontend/src/components/ui/ (available components: input, button, badge, separator, tooltip, scroll-area)
    - .planning/phases/02-ado-integration-prs-unified-dashboard/02-CONTEXT.md (D-25a through D-25h: External links and comments decisions)
  </read_first>
  <action>
**Create frontend/src/components/ExternalLinks.vue:**

Props:
```typescript
const props = defineProps<{ taskId: number }>()
```

State:
```typescript
const links = ref<TaskLink[]>([])
const newUrl = ref('')
const newLabel = ref('')
const loading = ref(false)

interface TaskLink {
    id: number
    taskId: number
    url: string
    label: string
    type: string // 'url', 'icm', 'grafana', 'ado', 'wiki'
    createdAt: string
}
```

On mount, fetch links:
```typescript
async function fetchLinks() {
    try {
        const { ListLinks } = await import('../../bindings/dev.azure.com/xbox/xb-tasks/internal/app/externallinksservice')
        links.value = await ListLinks(props.taskId) as TaskLink[]
    } catch { links.value = [] }
}
```

Add link:
```typescript
async function addLink() {
    const url = newUrl.value.trim()
    if (!url) return
    const { AddLink } = await import('../../bindings/dev.azure.com/xbox/xb-tasks/internal/app/externallinksservice')
    const link = await AddLink(props.taskId, url, newLabel.value.trim()) as TaskLink
    links.value.push(link)
    newUrl.value = ''
    newLabel.value = ''
}
```

Delete link:
```typescript
async function deleteLink(id: number) {
    const { DeleteLink } = await import('../../bindings/dev.azure.com/xbox/xb-tasks/internal/app/externallinksservice')
    await DeleteLink(id)
    links.value = links.value.filter(l => l.id !== id)
}
```

Type icon mapping (Lucide icons):
```typescript
const typeIcons: Record<string, Component> = {
    icm: AlertCircle,     // red — incident
    grafana: BarChart3,   // green — dashboard
    ado: GitPullRequest,  // blue — work item (or AzureDevOpsIcon)
    wiki: BookOpen,       // purple — wiki page
    url: ExternalLink,    // gray — generic link
}
```

Template:
```html
<div class="space-y-3">
  <div class="text-xs font-semibold text-muted-foreground uppercase">External Links</div>

  <!-- Link list -->
  <div v-for="link in links" :key="link.id"
       class="flex items-center gap-2 group px-2 py-1.5 rounded hover:bg-muted">
    <component :is="typeIcons[link.type] || ExternalLink"
               :size="14" :class="typeColors[link.type]" />
    <a :href="link.url" @click.prevent="openExternal(link.url)"
       class="text-sm text-foreground hover:underline truncate flex-1">
      {{ link.label || link.url }}
    </a>
    <Button variant="ghost" size="icon" class="h-6 w-6 opacity-0 group-hover:opacity-100"
            @click="deleteLink(link.id)">
      <X :size="12" />
    </Button>
  </div>

  <!-- Empty state -->
  <div v-if="!links.length" class="text-xs text-muted-foreground py-2">
    No links yet — add ICM, dashboard, or wiki URLs
  </div>

  <!-- Add link form -->
  <div class="flex gap-2">
    <Input v-model="newUrl" placeholder="Paste URL" class="h-7 text-xs flex-1"
           @keydown.enter="addLink" />
    <Input v-model="newLabel" placeholder="Label (optional)" class="h-7 text-xs w-24" />
    <Button variant="ghost" size="icon" class="h-7 w-7" @click="addLink" :disabled="!newUrl.trim()">
      <Plus :size="14" />
    </Button>
  </div>
</div>
```

Open external links in real browser (UX-02):
```typescript
function openExternal(url: string) {
    window.open(url, '_blank')
}
```

Type colors: `icm → text-red-500`, `grafana → text-green-500`, `ado → text-blue-500`, `wiki → text-purple-500`, `url → text-muted-foreground`.

**Create frontend/src/components/CommentsSection.vue:**

Props:
```typescript
const props = defineProps<{
    taskId: number
    isPublicTask: boolean  // whether the task is linked to ADO
}>()
```

State:
```typescript
const comments = ref<TaskComment[]>([])
const newComment = ref('')
const pushing = ref<number | null>(null) // comment ID being pushed

interface TaskComment {
    id: number; taskId: number; content: string;
    isPublic: boolean; adoCommentId: string;
    createdAt: string; updatedAt: string;
}
```

Methods:
```typescript
async function fetchComments() {
    const { ListComments } = await import('../../bindings/dev.azure.com/xbox/xb-tasks/internal/app/commentservice')
    comments.value = await ListComments(props.taskId) as TaskComment[]
}

async function addComment() {
    const content = newComment.value.trim()
    if (!content) return
    const { AddComment } = await import('../../bindings/dev.azure.com/xbox/xb-tasks/internal/app/commentservice')
    const c = await AddComment(props.taskId, content) as TaskComment
    comments.value.push(c)
    newComment.value = ''
}

async function pushToADO(commentId: number) {
    pushing.value = commentId
    try {
        const { PushCommentToADO } = await import('../../bindings/dev.azure.com/xbox/xb-tasks/internal/app/commentservice')
        await PushCommentToADO(commentId)
        // Mark as public locally
        const idx = comments.value.findIndex(c => c.id === commentId)
        if (idx !== -1) comments.value[idx].isPublic = true
    } finally { pushing.value = null }
}
```

Template:
```html
<div class="space-y-3">
  <div class="text-xs font-semibold text-muted-foreground uppercase">Comments</div>

  <ScrollArea class="max-h-60">
    <div v-for="c in comments" :key="c.id" class="px-2 py-2 border-b border-border last:border-0">
      <!-- Public indicator -->
      <div class="flex items-center gap-1 mb-1">
        <Badge v-if="c.isPublic" variant="outline" class="text-[10px] h-4 border-blue-500/30 text-blue-500">
          <Globe :size="10" class="mr-0.5" /> Public
        </Badge>
        <Badge v-else variant="outline" class="text-[10px] h-4 border-muted-foreground/30 text-muted-foreground">
          <Lock :size="10" class="mr-0.5" /> Private
        </Badge>
        <span class="text-[10px] text-muted-foreground ml-auto">
          {{ formatDate(c.createdAt) }}
        </span>
      </div>
      <!-- Content -->
      <div class="text-sm text-foreground whitespace-pre-wrap">{{ c.content }}</div>
      <!-- Push to ADO button (only for private comments on public tasks) -->
      <div v-if="!c.isPublic && isPublicTask" class="mt-1">
        <Button variant="ghost" size="sm" class="h-6 text-xs"
                @click="pushToADO(c.id)" :disabled="pushing === c.id">
          <Upload :size="12" class="mr-1" />
          {{ pushing === c.id ? 'Pushing...' : 'Push to ADO' }}
        </Button>
      </div>
    </div>
  </ScrollArea>

  <!-- Empty state -->
  <div v-if="!comments.length" class="text-xs text-muted-foreground py-2">
    No comments yet
  </div>

  <!-- Add comment -->
  <div class="flex gap-2">
    <Textarea v-model="newComment" placeholder="Add a comment..."
              class="text-xs min-h-[60px] resize-none" rows="2" />
  </div>
  <div class="flex justify-between items-center">
    <span class="text-[10px] text-muted-foreground">Comments are private by default</span>
    <Button size="sm" class="h-7 text-xs" @click="addComment" :disabled="!newComment.trim()">
      Add Comment
    </Button>
  </div>
</div>
```

Import: Badge, Button, ScrollArea, Textarea from shadcn-vue. Globe, Lock, Upload, Plus, X, ExternalLink, etc. from lucide-vue-next.
  </action>
  <verify>
    <automated>cd C:\code\xb-tasks\frontend && npx vue-tsc --noEmit 2>&1 | Select-String -Pattern "error" | Select-Object -First 5</automated>
  </verify>
  <acceptance_criteria>
    - frontend/src/components/ExternalLinks.vue exists and contains `AddLink`
    - frontend/src/components/ExternalLinks.vue contains `openExternal`
    - frontend/src/components/ExternalLinks.vue contains `window.open`
    - frontend/src/components/ExternalLinks.vue contains type icon mapping (icm, grafana, ado, wiki)
    - frontend/src/components/CommentsSection.vue exists and contains `AddComment`
    - frontend/src/components/CommentsSection.vue contains `PushCommentToADO`
    - frontend/src/components/CommentsSection.vue contains `Private` and `Public` labels
    - frontend/src/components/CommentsSection.vue contains `Push to ADO` button text
    - frontend/src/components/CommentsSection.vue contains `Comments are private by default`
  </acceptance_criteria>
  <done>ExternalLinks shows URLs with type icons (ICM, Grafana, ADO, Wiki) and opens in real browser. CommentsSection shows private/public distinction with selective ADO push button.</done>
</task>

<task type="auto">
  <name>Task 2: Integrate Links + Comments into TaskDetail</name>
  <files>frontend/src/components/TaskDetail.vue</files>
  <read_first>
    - frontend/src/components/TaskDetail.vue (full file — understand current layout, tabs, sections)
    - frontend/src/components/ExternalLinks.vue (just created — component props)
    - frontend/src/components/CommentsSection.vue (just created — component props)
    - frontend/src/stores/tasks.ts (isPublic function from Plan 06)
  </read_first>
  <action>
**Update frontend/src/components/TaskDetail.vue:**

Import the new components:
```typescript
import ExternalLinks from '@/components/ExternalLinks.vue'
import CommentsSection from '@/components/CommentsSection.vue'
import SyncConfirmDialog from '@/components/SyncConfirmDialog.vue'
import { useSyncStore } from '@/stores/sync'
import { useTaskStore } from '@/stores/tasks'

const syncStore = useSyncStore()
const taskStore = useTaskStore()
```

Add to the detail panel layout. The TaskDetail likely has tabs (Work / Discussion). Extend or add sections:

**If tabs exist (Work / Discussion):**
- Under "Work" tab: keep existing task fields + add ExternalLinks section at bottom
- Under "Discussion" tab: add CommentsSection
- Add new section or tab for "Sync" if task is linked:
  - Show current sync state (last synced, any pending changes)
  - "Push to ADO" button → calls `syncStore.generateOutboundDiff(task.id)`
  - "Unlink" button → calls LinkService.UnlinkTask

**If no tabs, add sections:**
After the main task fields (title, description, status, priority), add:
1. **ADO Link section** (only if task is public/linked):
   - Show linked ADO ID with type badge
   - "Push Changes" button
   - "Unlink from ADO" button
   - Last synced timestamp
2. **External Links** section:
   ```html
   <Separator class="my-3" />
   <ExternalLinks :task-id="task.id" />
   ```
3. **Comments** section:
   ```html
   <Separator class="my-3" />
   <CommentsSection :task-id="task.id" :is-public-task="taskStore.isPublic(task.id)" />
   ```

Also add the SyncConfirmDialog somewhere in the component tree (it's a Dialog, so position doesn't matter much):
```html
<SyncConfirmDialog />
```

Add a "Push to ADO" button that's visible only for linked tasks:
```html
<div v-if="taskStore.isPublic(task.id)" class="flex gap-2 mt-3">
  <Button variant="outline" size="sm" @click="syncStore.generateOutboundDiff(task.id)">
    <Upload :size="14" class="mr-1" />
    Push to ADO
  </Button>
</div>
```

Ensure the task detail re-fetches links and comments when the selected task changes (watch selectedTask).
  </action>
  <verify>
    <automated>cd C:\code\xb-tasks\frontend && npx vue-tsc --noEmit 2>&1 | Select-String -Pattern "error" | Select-Object -First 5</automated>
  </verify>
  <acceptance_criteria>
    - frontend/src/components/TaskDetail.vue contains `ExternalLinks` import
    - frontend/src/components/TaskDetail.vue contains `CommentsSection` import
    - frontend/src/components/TaskDetail.vue contains `<ExternalLinks` component usage
    - frontend/src/components/TaskDetail.vue contains `<CommentsSection` component usage
    - frontend/src/components/TaskDetail.vue contains `SyncConfirmDialog` import or usage
    - frontend/src/components/TaskDetail.vue contains `Push to ADO` or `generateOutboundDiff`
    - frontend/src/components/TaskDetail.vue contains `isPublic`
  </acceptance_criteria>
  <done>TaskDetail sidebar integrates ExternalLinks, CommentsSection, and sync controls. Push to ADO button visible for linked tasks. SyncConfirmDialog wired for outbound push preview.</done>
</task>

</tasks>

<verification>
- Frontend compiles
- TaskDetail shows links section with add/delete
- TaskDetail shows comments with private/public badges
- Push to ADO button visible for linked tasks
- External links open in real browser
</verification>

<success_criteria>
- Links displayed with type icons (LINK-03)
- External links open in browser, not in-app (UX-02)
- Comments local by default, pushable on demand (CMT-01, CMT-02)
- Public comments visually marked (CMT-02)
- Description push shows preview diff (CMT-03)
- All integrated into TaskDetail sidebar
</success_criteria>

<output>
After completion, create `.planning/phases/02-ado-integration-prs-unified-dashboard/02-09-SUMMARY.md`
</output>
