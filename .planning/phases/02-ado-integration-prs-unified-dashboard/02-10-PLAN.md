---
phase: 02-ado-integration-prs-unified-dashboard
plan: 10
type: execute
wave: 4
depends_on: [02-03, 02-04]
files_modified:
  - frontend/src/views/ProjectsView.vue
  - frontend/src/views/ProjectDetailView.vue
  - frontend/src/components/ProjectCard.vue
  - frontend/src/stores/projects.ts
  - frontend/src/router/index.ts
autonomous: true
requirements: [PROJ-01, PROJ-02, PROJ-03, PROJ-04, PROJ-05, PROJ-06, PROJ-07, UX-02]

must_haves:
  truths:
    - "Projects page shows card grid layout"
    - "Each card shows name, ADO badge (filled if linked), dual progress bars, task count"
    - "User can pin/star favorite projects to top of grid"
    - "Projects are flat (no sub-projects)"
    - "Projects can be local-only or linked to ADO scenarios/deliverables"
    - "Click card opens project dashboard with stats, ADO progress, and filtered tasks"
    - "Project dashboard shows two separate progress indicators: ADO children % and local task %"
    - "User can link/unlink project to ADO from project detail"
  artifacts:
    - path: "frontend/src/components/ProjectCard.vue"
      provides: "Project card with ADO badge and progress"
      contains: "isPinned"
    - path: "frontend/src/views/ProjectsView.vue"
      provides: "Card grid with pinned section"
      contains: "ProjectCard"
    - path: "frontend/src/views/ProjectDetailView.vue"
      provides: "Project dashboard with stats and tasks"
      contains: "ProjectProgress"
    - path: "frontend/src/stores/projects.ts"
      provides: "Extended project store with ADO linking"
      contains: "linkProjectToADO"
  key_links:
    - from: "frontend/src/stores/projects.ts"
      to: "Wails bindings"
      via: "ProjectService methods"
      pattern: "projectservice"
    - from: "frontend/src/views/ProjectDetailView.vue"
      to: "frontend/src/stores/tasks.ts"
      via: "Filtered tasks by project"
      pattern: "filterProject"
---

<objective>
Build the Projects page with card grid layout, project ADO linking, pin/star favorites, and project detail dashboard with dual progress indicators.

Purpose: Projects are the organizational layer — users group tasks under projects and optionally link projects to ADO scenarios/deliverables. The dual progress (local % + ADO %) gives a complete picture without mixing the two.

Output: `ProjectCard.vue`, updated `ProjectsView.vue`, new `ProjectDetailView.vue`, extended `projects.ts` store, updated router
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-ado-integration-prs-unified-dashboard/02-CONTEXT.md
@.planning/phases/02-ado-integration-prs-unified-dashboard/02-04-SUMMARY.md

<interfaces>
From internal/app/projects.go (Plan 04):
```go
func (s *ProjectService) Create(name, description string) (domain.Project, error)
func (s *ProjectService) List() ([]domain.Project, error)
func (s *ProjectService) Delete(id int) error
func (s *ProjectService) LinkProjectToADO(projectID int, adoID, direction string) error
func (s *ProjectService) UnlinkProject(projectID int, adoID string, deleteLocal bool) error
func (s *ProjectService) GetProjectADOLink(projectID int) (domain.ProjectADOLink, error)
func (s *ProjectService) PinProject(projectID int, pinned bool) error
func (s *ProjectService) GetProjectProgress(projectID int) (map[string]any, error)
// Returns: {"localDone": X, "localTotal": Y, "adoDone": A, "adoTotal": B}
```

From domain/types.go:
```go
type Project struct {
    ID int; Name string; Description string; Status ProjectStatus;
    IsPinned bool; CreatedAt time.Time; UpdatedAt time.Time;
}
type ProjectADOLink struct {
    ProjectID int; AdoID string; Direction string; CreatedAt time.Time;
}
```

From frontend/src/stores/projects.ts (existing):
```typescript
export interface Project {
    id: number; name: string; description: string; status: string;
    createdAt: string; updatedAt: string; taskCount?: number;
}
// Has: fetchProjects, createProject, deleteProject
```

From frontend/src/router/ (existing):
```
// Routes: /, /tasks, /ado, /projects, /settings, /login
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: ProjectCard + ProjectsView Card Grid</name>
  <files>frontend/src/components/ProjectCard.vue, frontend/src/views/ProjectsView.vue, frontend/src/stores/projects.ts</files>
  <read_first>
    - frontend/src/views/ProjectsView.vue (full file — current layout, understand what exists)
    - frontend/src/stores/projects.ts (full file — current store state and methods)
    - frontend/src/components/ui/card/ (shadcn Card component)
    - frontend/src/components/icons/ (check for AzureDevOpsIcon)
    - .planning/phases/02-ado-integration-prs-unified-dashboard/02-CONTEXT.md (PROJ-01, PROJ-02, D-20a: project decisions)
  </read_first>
  <action>
**Extend frontend/src/stores/projects.ts:**

Update Project interface:
```typescript
export interface Project {
    id: number
    name: string
    description: string
    status: string
    isPinned: boolean    // NEW
    createdAt: string
    updatedAt: string
    taskCount?: number
}

export interface ProjectADOLink {
    projectId: number
    adoId: string
    direction: string
    createdAt: string
}

export interface ProjectProgress {
    localDone: number
    localTotal: number
    adoDone: number
    adoTotal: number
}
```

Add state and methods:
```typescript
const projectLinks = ref<Map<number, ProjectADOLink>>(new Map())
const projectProgress = ref<Map<number, ProjectProgress>>(new Map())

async function pinProject(id: number, pinned: boolean) {
    const { PinProject } = await import('../../bindings/dev.azure.com/xbox/xb-tasks/internal/app/projectservice')
    await PinProject(id, pinned)
    const idx = projects.value.findIndex(p => p.id === id)
    if (idx !== -1) projects.value[idx].isPinned = pinned
}

async function linkProjectToADO(projectId: number, adoId: string) {
    const { LinkProjectToADO } = await import('../../bindings/dev.azure.com/xbox/xb-tasks/internal/app/projectservice')
    await LinkProjectToADO(projectId, adoId, 'linked')
}

async function unlinkProject(projectId: number, adoId: string) {
    const { UnlinkProject } = await import('../../bindings/dev.azure.com/xbox/xb-tasks/internal/app/projectservice')
    await UnlinkProject(projectId, adoId, false) // keep local
    projectLinks.value.delete(projectId)
}

async function fetchProjectLink(projectId: number) {
    try {
        const { GetProjectADOLink } = await import('../../bindings/dev.azure.com/xbox/xb-tasks/internal/app/projectservice')
        const link = await GetProjectADOLink(projectId) as ProjectADOLink
        if (link && link.adoId) projectLinks.value.set(projectId, link)
    } catch { /* no link */ }
}

async function fetchProjectProgress(projectId: number) {
    try {
        const { GetProjectProgress } = await import('../../bindings/dev.azure.com/xbox/xb-tasks/internal/app/projectservice')
        const progress = await GetProjectProgress(projectId) as ProjectProgress
        projectProgress.value.set(projectId, progress)
    } catch { /* ignore */ }
}

const pinnedProjects = computed(() => projects.value.filter(p => p.isPinned))
const unpinnedProjects = computed(() => projects.value.filter(p => !p.isPinned))

function isLinked(projectId: number): boolean {
    return projectLinks.value.has(projectId)
}
```

Export all new state and methods.

**Create frontend/src/components/ProjectCard.vue:**

```typescript
const props = defineProps<{
    project: Project
    isLinked: boolean
    progress: ProjectProgress | null
}>()

const emit = defineEmits<{
    'click': []
    'pin': [pinned: boolean]
}>()
```

Template — card layout (PROJ-01):
```html
<Card class="cursor-pointer hover:border-primary/50 transition-colors" @click="emit('click')">
  <CardHeader class="pb-2">
    <div class="flex items-center justify-between">
      <CardTitle class="text-sm font-semibold">{{ project.name }}</CardTitle>
      <div class="flex items-center gap-1">
        <!-- Pin button -->
        <Button variant="ghost" size="icon" class="h-6 w-6"
                @click.stop="emit('pin', !project.isPinned)">
          <Star :size="14" :class="project.isPinned ? 'fill-amber-400 text-amber-400' : 'text-muted-foreground'" />
        </Button>
        <!-- ADO badge -->
        <div v-if="isLinked"
             class="w-5 h-5 rounded-full bg-blue-500/10 flex items-center justify-center"
             title="Linked to ADO">
          <AzureDevOpsIcon class="w-3 h-3 text-blue-500" />
        </div>
        <div v-else
             class="w-5 h-5 rounded-full border border-dashed border-muted-foreground/30 flex items-center justify-center"
             title="Local only">
        </div>
      </div>
    </div>
  </CardHeader>
  <CardContent class="pb-3 space-y-2">
    <!-- Task count -->
    <div class="text-xs text-muted-foreground">
      {{ progress?.localTotal ?? 0 }} tasks
    </div>

    <!-- Dual progress bars -->
    <div v-if="progress" class="space-y-1.5">
      <!-- Local task progress -->
      <div class="flex items-center gap-2">
        <span class="text-[10px] text-muted-foreground w-12">Local</span>
        <div class="flex-1 h-1.5 rounded-full bg-muted">
          <div class="h-full rounded-full bg-primary transition-all"
               :style="{ width: localPercent + '%' }"></div>
        </div>
        <span class="text-[10px] text-muted-foreground w-8 text-right">{{ localPercent }}%</span>
      </div>
      <!-- ADO progress (only if linked) -->
      <div v-if="isLinked" class="flex items-center gap-2">
        <span class="text-[10px] text-muted-foreground w-12">ADO</span>
        <div class="flex-1 h-1.5 rounded-full bg-muted">
          <div class="h-full rounded-full bg-blue-500 transition-all"
               :style="{ width: adoPercent + '%' }"></div>
        </div>
        <span class="text-[10px] text-muted-foreground w-8 text-right">{{ adoPercent }}%</span>
      </div>
    </div>
  </CardContent>
</Card>
```

Computed percents:
```typescript
const localPercent = computed(() => {
    if (!props.progress || !props.progress.localTotal) return 0
    return Math.round((props.progress.localDone / props.progress.localTotal) * 100)
})
const adoPercent = computed(() => {
    if (!props.progress || !props.progress.adoTotal) return 0
    return Math.round((props.progress.adoDone / props.progress.adoTotal) * 100)
})
```

Import: Card/CardHeader/CardTitle/CardContent from shadcn, Star from lucide-vue-next, AzureDevOpsIcon.

**Overhaul frontend/src/views/ProjectsView.vue:**

Card grid layout with pinned section:
```html
<div class="p-6 space-y-6">
  <PageHeader title="Projects">
    <Button @click="showCreate = true" size="sm">
      <Plus :size="14" class="mr-1" /> New Project
    </Button>
  </PageHeader>

  <!-- Pinned projects -->
  <div v-if="projectStore.pinnedProjects.length">
    <div class="text-xs font-semibold text-muted-foreground uppercase mb-2">Pinned</div>
    <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-4">
      <ProjectCard
        v-for="p in projectStore.pinnedProjects" :key="p.id"
        :project="p"
        :is-linked="projectStore.isLinked(p.id)"
        :progress="projectStore.projectProgress.get(p.id) ?? null"
        @click="router.push('/projects/' + p.id)"
        @pin="projectStore.pinProject(p.id, $event)"
      />
    </div>
  </div>

  <!-- All projects -->
  <div>
    <div class="text-xs font-semibold text-muted-foreground uppercase mb-2">All Projects</div>
    <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-4">
      <ProjectCard
        v-for="p in projectStore.unpinnedProjects" :key="p.id"
        :project="p"
        :is-linked="projectStore.isLinked(p.id)"
        :progress="projectStore.projectProgress.get(p.id) ?? null"
        @click="router.push('/projects/' + p.id)"
        @pin="projectStore.pinProject(p.id, $event)"
      />
    </div>
  </div>
</div>
```

On mount: fetch projects, then for each project fetch link + progress data.
  </action>
  <verify>
    <automated>cd C:\code\xb-tasks\frontend && npx vue-tsc --noEmit 2>&1 | Select-String -Pattern "error" | Select-Object -First 5</automated>
  </verify>
  <acceptance_criteria>
    - frontend/src/components/ProjectCard.vue exists and contains `isPinned`
    - frontend/src/components/ProjectCard.vue contains ADO badge (filled/hollow pattern)
    - frontend/src/components/ProjectCard.vue contains dual progress bars (Local + ADO)
    - frontend/src/components/ProjectCard.vue contains `Star` icon for pin
    - frontend/src/views/ProjectsView.vue contains `ProjectCard`
    - frontend/src/views/ProjectsView.vue contains `grid` layout (card grid)
    - frontend/src/views/ProjectsView.vue contains `pinnedProjects`
    - frontend/src/stores/projects.ts contains `pinProject`
    - frontend/src/stores/projects.ts contains `linkProjectToADO`
    - frontend/src/stores/projects.ts contains `ProjectProgress` interface
    - frontend/src/stores/projects.ts contains `isPinned` in Project interface
  </acceptance_criteria>
  <done>ProjectCard shows name, ADO badge, dual progress bars, task count, and pin/star. ProjectsView renders card grid with pinned section at top. Store supports pin, ADO link, and progress fetching.</done>
</task>

<task type="auto">
  <name>Task 2: ProjectDetailView + Router Update</name>
  <files>frontend/src/views/ProjectDetailView.vue, frontend/src/router/index.ts</files>
  <read_first>
    - frontend/src/router/index.ts (current routes — understand structure, add project detail route)
    - frontend/src/views/TasksView.vue (understand how task filtering works — we filter by project)
    - frontend/src/stores/projects.ts (extended store from Task 1)
    - frontend/src/stores/tasks.ts (filterProject from Plan 06)
    - .planning/phases/02-ado-integration-prs-unified-dashboard/02-CONTEXT.md (PROJ-05, PROJ-06: project dashboard decisions)
  </read_first>
  <action>
**Create frontend/src/views/ProjectDetailView.vue:**

Project dashboard showing stats, ADO context, and filtered tasks (PROJ-05).

```typescript
import { ref, computed, onMounted, watch } from 'vue'
import { useRoute, useRouter } from 'vue-router'
import { useProjectStore } from '@/stores/projects'
import { useTaskStore } from '@/stores/tasks'
import { useSyncStore } from '@/stores/sync'
```

State:
```typescript
const route = useRoute()
const router = useRouter()
const projectStore = useProjectStore()
const taskStore = useTaskStore()
const projectId = computed(() => Number(route.params.id))
const project = computed(() => projectStore.projects.find(p => p.id === projectId.value))
const progress = computed(() => projectStore.projectProgress.get(projectId.value))
const adoLink = computed(() => projectStore.projectLinks.get(projectId.value))
const showLinkDialog = ref(false)
```

Layout:
```html
<div class="p-6 space-y-6">
  <!-- Back button + title -->
  <div class="flex items-center gap-3">
    <Button variant="ghost" size="icon" @click="router.push('/projects')">
      <ArrowLeft :size="16" />
    </Button>
    <div>
      <h1 class="text-lg font-semibold">{{ project?.name }}</h1>
      <p class="text-sm text-muted-foreground">{{ project?.description }}</p>
    </div>
    <div class="ml-auto flex gap-2">
      <!-- ADO link/unlink -->
      <Button v-if="!adoLink" variant="outline" size="sm" @click="showLinkDialog = true">
        Link to ADO
      </Button>
      <Button v-else variant="outline" size="sm" @click="handleUnlink">
        Unlink from ADO
      </Button>
    </div>
  </div>

  <!-- Stats cards row -->
  <div class="grid grid-cols-2 md:grid-cols-4 gap-4">
    <Card>
      <CardContent class="pt-4">
        <div class="text-2xl font-bold">{{ progress?.localTotal ?? 0 }}</div>
        <div class="text-xs text-muted-foreground">Total Tasks</div>
      </CardContent>
    </Card>
    <Card>
      <CardContent class="pt-4">
        <div class="text-2xl font-bold text-green-500">{{ progress?.localDone ?? 0 }}</div>
        <div class="text-xs text-muted-foreground">Completed</div>
      </CardContent>
    </Card>
    <Card v-if="adoLink">
      <CardContent class="pt-4">
        <div class="text-2xl font-bold text-blue-500">{{ progress?.adoTotal ?? 0 }}</div>
        <div class="text-xs text-muted-foreground">ADO Children</div>
      </CardContent>
    </Card>
    <Card v-if="adoLink">
      <CardContent class="pt-4">
        <div class="text-2xl font-bold text-blue-500">{{ adoPercent }}%</div>
        <div class="text-xs text-muted-foreground">ADO Progress</div>
      </CardContent>
    </Card>
  </div>

  <!-- Dual progress bars (PROJ-06) -->
  <div class="space-y-2">
    <div class="flex items-center gap-3">
      <span class="text-sm font-medium w-20">Local Tasks</span>
      <div class="flex-1 h-2 rounded-full bg-muted">
        <div class="h-full rounded-full bg-primary" :style="{ width: localPercent + '%' }"></div>
      </div>
      <span class="text-sm text-muted-foreground">{{ localPercent }}%</span>
    </div>
    <div v-if="adoLink" class="flex items-center gap-3">
      <span class="text-sm font-medium w-20">ADO Items</span>
      <div class="flex-1 h-2 rounded-full bg-muted">
        <div class="h-full rounded-full bg-blue-500" :style="{ width: adoPercent + '%' }"></div>
      </div>
      <span class="text-sm text-muted-foreground">{{ adoPercent }}%</span>
    </div>
  </div>

  <!-- ADO context (if linked) -->
  <Card v-if="adoLink">
    <CardHeader class="pb-2">
      <CardTitle class="text-sm">ADO Context</CardTitle>
    </CardHeader>
    <CardContent>
      <div class="text-sm">Linked to ADO item {{ adoLink.adoId }} ({{ adoLink.direction }})</div>
      <Button variant="link" class="h-auto p-0 text-sm"
              @click="window.open(adoWebUrl, '_blank')">
        Open in ADO →
      </Button>
    </CardContent>
  </Card>

  <!-- Filtered tasks for this project -->
  <div>
    <div class="text-sm font-semibold mb-2">Tasks</div>
    <!-- Render task rows filtered to this project -->
    <TaskRow
      v-for="task in projectTasks" :key="task.id"
      :task="task"
      :is-public="taskStore.isPublic(task.id)"
      @click="taskStore.selectTask(task.id)"
    />
    <div v-if="!projectTasks.length" class="text-sm text-muted-foreground py-4 text-center">
      No tasks in this project yet
    </div>
  </div>
</div>
```

Computed:
```typescript
const projectTasks = computed(() =>
    taskStore.tasks.filter(t => t.projectId === projectId.value)
)
const localPercent = computed(() => {
    if (!progress.value?.localTotal) return 0
    return Math.round((progress.value.localDone / progress.value.localTotal) * 100)
})
const adoPercent = computed(() => {
    if (!progress.value?.adoTotal) return 0
    return Math.round((progress.value.adoDone / progress.value.adoTotal) * 100)
})
```

On mount: fetch project data, progress, ADO link, and tasks.

**Update frontend/src/router/index.ts:**

Add project detail route:
```typescript
{
    path: '/projects/:id',
    name: 'project-detail',
    component: () => import('@/views/ProjectDetailView.vue'),
    meta: { requiresAuth: true }
}
```

This should be added alongside the existing `/projects` route.
  </action>
  <verify>
    <automated>cd C:\code\xb-tasks\frontend && npx vue-tsc --noEmit 2>&1 | Select-String -Pattern "error" | Select-Object -First 5</automated>
  </verify>
  <acceptance_criteria>
    - frontend/src/views/ProjectDetailView.vue exists
    - frontend/src/views/ProjectDetailView.vue contains dual progress bars (Local + ADO)
    - frontend/src/views/ProjectDetailView.vue contains `Link to ADO` button
    - frontend/src/views/ProjectDetailView.vue contains `ADO Context` section
    - frontend/src/views/ProjectDetailView.vue contains filtered tasks rendering
    - frontend/src/views/ProjectDetailView.vue contains stats cards (Total, Completed, ADO)
    - frontend/src/router/index.ts contains `/projects/:id` route
    - frontend/src/router/index.ts contains `ProjectDetailView` import
  </acceptance_criteria>
  <done>ProjectDetailView shows stats cards, dual progress bars, ADO context, and filtered task list. Route added for /projects/:id. Link/unlink to ADO supported from detail view.</done>
</task>

</tasks>

<verification>
- Frontend compiles
- Projects page renders card grid
- Project cards show ADO badge and dual progress
- Project detail shows stats, progress, tasks
- Pin/star moves projects to top
- Link/unlink to ADO works from project detail
</verification>

<success_criteria>
- Card grid layout (PROJ-01)
- Pin/star favorites to top (PROJ-02)
- Projects flat, no sub-projects (PROJ-03)
- Local-only or ADO-linked (PROJ-04)
- Project dashboard with stats and filtered tasks (PROJ-05)
- Dual progress: local + ADO, kept separate (PROJ-06)
- Link/unlink project to ADO (PROJ-07)
</success_criteria>

<output>
After completion, create `.planning/phases/02-ado-integration-prs-unified-dashboard/02-10-SUMMARY.md`
</output>
