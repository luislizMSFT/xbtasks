---
phase: 01
plan: 06
type: auto
title: "Frontend — Dashboard, Tasks & Projects Views"
wave: 4
depends_on: ["01-05-PLAN"]
files_modified: ["frontend/src/views/DashboardView.vue", "frontend/src/views/TasksView.vue", "frontend/src/views/ProjectsView.vue"]
requirements: ["TASK-01", "TASK-02", "TASK-03", "TASK-04", "TASK-05", "TASK-06", "TASK-07"]
autonomous: true
must_haves:
  truths:
    - "DashboardView with stat cards, Today's Focus, and Recent Activity sections"
    - "TasksView with task table, status tabs, create form, and slide-out detail panel"
    - "ProjectsView with card grid and create project form"
    - "Empty states use UI-SPEC copywriting for all three views"
    - "Task creation via form with title, description, priority, category, tags"
  artifacts:
    - path: "frontend/src/views/DashboardView.vue"
      provides: "Landing page with stats, today's focus, recent activity"
    - path: "frontend/src/views/TasksView.vue"
      provides: "Primary task management view with table + detail panel"
    - path: "frontend/src/views/ProjectsView.vue"
      provides: "Project card grid with CRUD"
---

# Plan 06: Frontend — Dashboard, Tasks & Projects Views

<objective>
Build the three main application views — Dashboard (landing page), Tasks (primary workspace with table + detail panel), and Projects (organizational containers). These consume the UI primitives and Pinia store from Plan 05.
</objective>

<tasks>

<task id="06.1">
<title>Create DashboardView</title>
<read_first>
- .planning/phases/01-foundation-auth-personal-tasks/01-UI-SPEC.md (Dashboard View section)
- frontend/src/stores/tasks.ts (stats computed)
- frontend/src/components/ui/StatusBadge.vue (for task rows)
- frontend/src/components/TaskRow.vue (for Today's Focus section)
</read_first>
<action>
Replace the placeholder `frontend/src/views/DashboardView.vue` with the full implementation:

Per UI-SPEC Dashboard layout:
- Page header: "Dashboard" (20px, 600 weight) + "+ Create Task" primary button (right-aligned)
- **Stat cards row** (focal point — per UI-SPEC): 4 cards in a row showing Total, In Progress, Blocked, Done counts
  - Card: `var(--color-bg-secondary)` bg, 16px padding, rounded-lg, `1px solid var(--color-border)`
  - Value: 28px font, 600 weight, `var(--color-text-primary)`
  - Label: 12px font, 600 weight, `var(--color-text-secondary)`
  - Grid: `grid grid-cols-4 gap-4`
- **Today's Focus section** (heading 14px, 600 weight): Tasks with status `in_progress`, displayed as compact TaskRow components
  - Empty state: "No tasks in progress — pick something to work on." (14px, 400 weight, `var(--color-text-tertiary)`)
- **Recent Activity section** (heading 14px, 600 weight): Last 5 updated tasks, compact list with StatusBadge and relative timestamp
  - Use `tasks.value.slice().sort((a,b) => new Date(b.updatedAt) - new Date(a.updatedAt)).slice(0, 5)` for recent items

Fetch tasks on mount: `onMounted(() => taskStore.fetchTasks())`
Use `taskStore.stats` computed for stat card values.
"+ Create Task" button navigates to `/tasks` with a query param to auto-open create form, or emits a global event.
</action>
<acceptance_criteria>
- `frontend/src/views/DashboardView.vue` exists and is more than 20 lines
- `grep 'Dashboard' frontend/src/views/DashboardView.vue` returns a match
- `grep 'stats\|stat' frontend/src/views/DashboardView.vue` returns at least 1 match
- `grep 'Create Task' frontend/src/views/DashboardView.vue` returns a match
- `grep 'No tasks in progress' frontend/src/views/DashboardView.vue` returns a match
- `grep 'color-bg-secondary\|--color-bg' frontend/src/views/DashboardView.vue` returns at least 1 match
</acceptance_criteria>
</task>

<task id="06.2">
<title>Create TasksView with table and detail panel</title>
<read_first>
- .planning/phases/01-foundation-auth-personal-tasks/01-UI-SPEC.md (Tasks View section, Task Table Row section)
- frontend/src/components/TaskRow.vue
- frontend/src/components/TaskDetail.vue
- frontend/src/stores/tasks.ts
</read_first>
<action>
Replace the placeholder `frontend/src/views/TasksView.vue` with the full implementation:

Per UI-SPEC Tasks View layout:
- Page header: "Tasks" (20px, 600 weight) + "+ Create Task" primary button
- **Status tabs**: All, Todo, In Progress, In Review, Done, Blocked
  - 14px font, 600 weight for active, 400 for inactive
  - 8px vertical padding, active tab has 2px `var(--color-accent)` bottom border
  - Click sets `taskStore.statusFilter`
- **Task table** (focal point — per UI-SPEC): List of TaskRow components
  - Header row: Title, Status, Priority, Category, Updated — 12px font, 600 weight, `var(--color-text-secondary)`
  - 12px horizontal padding per cell, 12px vertical padding per row
  - Sorted by priority (P0 first) then updated_at DESC (matches Go service order)
  - Empty state: heading "No tasks yet" (20px, 600 weight) + body "Create your first task to get started. Press ⌘N or click the button above." (14px, 400 weight)
- **Task detail panel**: Slides in from right when a task is selected
  - Width: 40% of content area per UI-SPEC
  - Uses TaskDetail component from Plan 05
  - `var(--color-bg-secondary)` background, `1px solid var(--color-border)` left border
  - Close on X button or Escape key
  - Transition: `transform 200ms ease-out` (per UI-SPEC interaction patterns)

**Create task flow**: "+ Create Task" button toggles an inline form at the top of the task table area. Fields:
  - Title (required, 14px font, full width input)
  - Description (optional textarea)
  - Priority dropdown (P0-P3, default P2)
  - Category text input
  - Tags text input (comma-separated)
  - "Create Task" submit button + "Cancel" ghost button
  - On submit: `taskStore.createTask(...)`, close form, new task appears at top of list

**Keyboard shortcuts** (via @vueuse/core useMagicKeys):
  - ⌘N / Ctrl+N: Toggle create task form
  - Escape: Close detail panel (if open) or close create form (if open)
</action>
<acceptance_criteria>
- `frontend/src/views/TasksView.vue` exists and is more than 40 lines
- `grep 'TaskRow\|TaskDetail' frontend/src/views/TasksView.vue` returns at least 2 matches
- `grep 'statusFilter\|status' frontend/src/views/TasksView.vue` returns at least 1 match
- `grep 'No tasks yet' frontend/src/views/TasksView.vue` returns a match
- `grep 'Create Task\|createTask' frontend/src/views/TasksView.vue` returns at least 1 match
- `grep 'useMagicKeys\|Meta_n\|Ctrl_n' frontend/src/views/TasksView.vue` returns at least 1 match
</acceptance_criteria>
</task>

<task id="06.3">
<title>Create ProjectsView</title>
<read_first>
- .planning/phases/01-foundation-auth-personal-tasks/01-UI-SPEC.md (Projects View section)
- internal/app/projects.go (Go ProjectService methods)
</read_first>
<action>
Replace the placeholder `frontend/src/views/ProjectsView.vue` with the full implementation:

Per UI-SPEC Projects View:
- Page header: "Projects" (20px, 600 weight) + "+ Create Project" primary button
- **Card grid layout**: `grid grid-cols-2 lg:grid-cols-3 gap-4`
  - Each card: project name (14px, 600 weight), description (12px, 400 weight, `var(--color-text-secondary)`, max 2 lines), status badge, task count
  - Card: 16px padding, `var(--color-bg-secondary)` bg, rounded-lg, `1px solid var(--color-border)`
  - Hover: `box-shadow: 0 2px 8px rgba(0,0,0,0.1)` (subtle elevation)
  - Click: navigate to tasks view filtered by project (future enhancement — for now, just highlight)
- **Empty state**: "No projects yet" heading (20px, 600 weight) + "Create a project to organize related tasks." body (14px, 400 weight)
- **Create project flow**: "+ Create Project" opens inline form or modal
  - Name (required), Description (optional)
  - On submit: calls `window.go.app.ProjectService.Create(name, description)` via Wails binding

Create a simple Pinia store or inline reactive state for projects:
```typescript
const projects = ref([])
async function fetchProjects() {
  projects.value = await (window as any).go.app.ProjectService.List()
}
onMounted(fetchProjects)
```
</action>
<acceptance_criteria>
- `frontend/src/views/ProjectsView.vue` exists and is more than 20 lines
- `grep 'Project\|project' frontend/src/views/ProjectsView.vue` returns at least 2 matches
- `grep 'No projects yet' frontend/src/views/ProjectsView.vue` returns a match
- `grep 'Create.*project\|ProjectService' frontend/src/views/ProjectsView.vue` returns at least 1 match
- `grep 'color-bg-secondary\|--color-bg' frontend/src/views/ProjectsView.vue` returns at least 1 match
</acceptance_criteria>
</task>

</tasks>

<verification>
```bash
cd /Users/luisliz/knowledge/code/team-ado-tool/frontend
npx vite build
```
Frontend builds successfully with all views consuming components from Plan 05.
</verification>

<must_haves>
- DashboardView with stat cards (Total, In Progress, Blocked, Done), Today's Focus, Recent Activity
- TasksView with status filter tabs, task table using TaskRow, slide-out TaskDetail panel, inline create form
- ProjectsView with card grid layout, create project form, empty state
- All empty states use exact UI-SPEC copywriting
- Keyboard shortcuts (⌘N for create task, Escape for close panels)
- CSS custom properties for all colors (theme-aware)
</must_haves>
